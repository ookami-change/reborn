# 系统架构

> 版本：v1 · 2026-09-02
> 相关文档：《作业批改与错题管理App市场调研及可行性分析.md》《模型准确率基准测试方案.md》
> 约束：单人业余（每周 10–15h）、首批用户是自家孩子 + 家长群、练手/技术验证定位。

---

## 一、路线调整：错题管理先行

上一版方案把「模型批改准确率」当成了整个项目的前置门槛。这个排序是错的。

**错题管理不在批改的依赖链上。** 家长本来就在判对错——这件事今天不用任何 App 也在发生。系统真正缺的是「判完之后怎么办」：记下来、到期提醒、组成卷子、重做、确认掌握。这一整条链路**零模型依赖，全是工程逻辑**。

调整后的关系：

| | 原排序 | 现排序 |
|---|---|---|
| 错题管理 | 等 benchmark 结果 | **主线，立刻开始** |
| 模型批改 | 前置门槛 | **输入层的一个可选加速器** |
| benchmark | 决定项目生死 | 决定输入层能自动到哪一级 |

还有一个反向收益：**MVP 上线后，家长的每一次手动圈选和判定，都是一条 benchmark 标注数据。** 标注从「专门花四个晚上做的苦活」变成了「使用的副产品」。所以顺序应该是：先做 MVP → 真实使用产出标注集 → 再跑 benchmark。

---

## 二、分层架构与那条稳定接口

整个系统的架构要点只有一个：**把「错题是怎么产生的」和「错题产生之后怎么办」彻底隔开。**

```
┌─────────────────────────────────────────────────────────┐
│  输入层（自动化程度可插拔）                                  │
│                                                          │
│   L0  拍照 → 家长手动框选错题 → 手动填正确答案      ← MVP    │
│   L1  拍照 → 自动切题 → 家长点选哪几道错了        ← 需检出模型│
│   L2  拍照 → 自动切题 + 自动判对错 → 家长确认      ← 需批改模型│
└──────────────────────┬──────────────────────────────────┘
                       │
         ╔═════════════▼═══════════════════════════════╗
         ║  稳定接口：Attempt                            ║
         ║  { problem_crop, child_answer, correct_answer,║
         ║    verdict, source, confidence }              ║
         ╚═════════════┬═══════════════════════════════╝
                       │
┌──────────────────────▼──────────────────────────────────┐
│  领域层（纯工程，零模型）                                    │
│   · 错题卡生成与去重                                       │
│   · 掌握状态机                                            │
│   · 复习调度器（Leitner 盒子）                              │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│  输出层                                                   │
│   · 首页「今晚要做什么」                                    │
│   · 复习卷渲染（PDF / PNG，带回收二维码）                     │
│   · 家长周报                                              │
└─────────────────────────────────────────────────────────┘
```

**这条接口画对了，benchmark 的结果就是一个插件级决策，而不是一次重写。** L0/L1/L2 三级只改变 Attempt 是怎么被创造出来的；下游一行都不用动。

反过来说，如果第一版图省事，让 UI 直接写数据库、把「家长点了叉」和「错题卡」揉成一张表，那么等模型能力接进来时，整个数据流要重做。**这是第一版最值得花半小时想清楚、也最容易偷懒的地方。**

---

## 三、领域模型

### 3.1 实体

| 实体 | 说明 | 关键字段 |
|---|---|---|
| **Child** 孩子 | 从第一天就允许多个，字段成本几乎为零 | `id, 昵称, 年级` |
| **Capture** 拍摄 | 一次拍照事件（一页作业 / 一张试卷 / 一次复习卷重做） | `id, child_id, 图片, 时间, 来源类型, review_sheet_id?` |
| **Problem** 题目 | 一道题的身份。**题干裁剪图是必填，文本是可选** | `id, child_id, 裁剪图, 题干文本?, 正确答案, 首次来源capture_id` |
| **Attempt** 作答 | 某道题的**一次**作答记录。同一道题会有多条 | `id, problem_id, capture_id, 孩子答案, verdict, source, confidence, 时间` |
| **MistakeCard** 错题卡 | 复习调度的单位，与 Problem 一对一 | `id, problem_id, 状态, box层级, 下次到期日, 连续答对次数` |
| **ReviewSheet** 复习卷 | 一次生成的卷子 | `id, child_id, 生成时间, 题目列表(有序), 短码, 完成状态` |

### 3.2 为什么必须把 Problem / Attempt / MistakeCard 拆成三个

最容易犯的错是：把「错题」做成一张表就完事。

问题出在**重做**。同一道题会被做很多次（原始 → 复习1 → 复习2 → ...），每一次都需要记录：什么时候做的、这次答案是什么、对还是错。这些是 Attempt。

如果只有一张「错题表」，重做记录就没地方放，掌握状态只能硬编成表上的一个 `连续答对次数` 计数器。后果是：

- 想改掌握判定规则（从「连对2次」改成「间隔7天后仍答对」）→ 历史数据无法重算
- 想看「这孩子的错误模式」→ 没有时间序列可分析
- 想做家长周报「本周重做了几道、对了几道」→ 数据不存在

**掌握状态必须是 Attempt 序列的函数，而不是一个被就地覆盖的计数器。** 这是第一版最值得多花二十分钟的设计。

### 3.3 题干截图优先，OCR 文本可选

`Problem.裁剪图` 必填、`Problem.题干文本` 可空。组卷时**直接排版截图**，不重新渲染文字。

这一条同时解决三件事：
- 第一版**完全不需要 OCR**，即零模型依赖
- 数学公式、竖式、几何图形、表格不会在重排版时变形——这正是竞品被骂最多的点
- 后续 OCR 接进来时，是给 Problem 补一个可选字段，不是改数据结构

### 3.4 v1 不做知识点标注

不强制家长打标签、选科目、选章节。**竞品用户投诉最集中的就是「录入步骤多」，别一上来就复制这个错误。**

v1 只按「题目 + 时间 + 来源作业」归类，够用了。知识点归类是 v2 的模型能力（模型看题干自动打标），不是让家长手工干的活。

---

## 四、复习调度与掌握状态

### 4.1 用 Leitner 盒子，不要用 FSRS/SM-2

原调研文档写的 1/3/7/14/30 天间隔本质就是 Leitner。这是对的，而且**不要升级成 SM-2 或 FSRS**。

理由是这些算法的前提假设在这里不成立：

| SRS 算法假设 | 本场景实际 |
|---|---|
| 每天打卡复习 | 家长一周组一次卷，**批量执行** |
| 单卡独立调度 | 一张卷子上的题必须凑够一定数量才值得打印 |
| 用户自评「难/易」 | 孩子不会自评，只有「对/错」二元信号 |
| 目标是长期记忆保持 | 目标是**知识点掌握**，掌握后就该退出，不是无限期维持 |

Leitner 盒子简单、可解释、能跟家长讲清楚，而且**能容忍"这周没做"这种真实情况**（题留在原盒子里，不会像 SRS 那样堆积成惩罚性的欠债）。

### 4.2 状态机

```
   新错题 ──→ Box1(1天) ──答对──→ Box2(3天) ──答对──→ Box3(7天)
                 ↑                    │                  │
                 │                   答错                答对
                 └────────────────────┴──────────────────┤
                                                          ▼
                              Box4(14天) ──答对──→ Box5(30天) ──答对──→ 已掌握
```

- 答错一律回 Box1
- 到 Box5 后再答对 → 标记「已掌握」，退出常规复习
- 已掌握的题保留在库里，可以在「月度回顾卷」里低频抽查

### 4.3 假掌握问题

孩子可能是**记住了答案**而不是会做——尤其题干截图一模一样时。

- v1 的应对：**靠拉长间隔**（30天后还能做对，多半是真会了）
- v2 的应对：生成变式题（换数字、换情境）——这需要模型，属于后续
- 别在 v1 假装解决这个问题，但要在数据上留出观察它的能力（看 Box5 → 已掌握 之后是否重新出错）

---

## 五、输入层的三级自动化

| 级别 | 家长要做什么 | 依赖 | 每道错题耗时 |
|---|---|---|---|
| **L0 手动框选** | 拍照 → 拖框圈住错题 → 输正确答案 | 无 | ~15s |
| **L1 自动切题** | 拍照 → 系统切好所有题 → **点一下**哪几道错了 | 检出模型 | ~3s |
| **L2 自动批改** | 拍照 → 系统标好对错 → 扫一眼确认 | 完整批改模型 | ~0s（但要看一遍） |

### 一个能省掉一周前端工作量的判断

**L0 里最难的不是后端，是手机上的图片框选交互。** 缩放、拖动、多框、编辑已有框、在小屏上精确对齐——这一块能吃掉一整周，而后端全是 CRUD，一晚上写完。

**而 L1 让这块交互几乎完全消失**：如果自动切题可靠，家长只需要在切好的题上「点一下」，不用拖任何框。

所以有一个高杠杆动作：**先花一个晚上，只测「自动切题」这一项**（不测判对错）。检出比判对错容易得多，很可能直接可用。

- 检出可靠 → 输入层直接按 L1 设计，**省掉最难的那周前端**
- 检出不可靠 → 老老实实做 L0 框选交互

**一个晚上的测试，决定要不要写一周的代码。这是目前投入产出比最高的一件事。**

---

## 六、输出层

### 6.1 复习卷带回收二维码 ← 关键设计

孩子做完复习卷、拍照回来，系统怎么知道这是哪张卷、拍的是哪几道题？

**不要试图靠识别去解决。** 组卷时就把答案印在纸上：

- 每张复习卷右上角印一个**二维码**，编码 `sheet_id`
- 每道题旁边印一个**短码**（如 `07`）
- 回收时扫码 → 系统立刻知道这张卷有哪些题、什么顺序 → 家长按顺序点对错即可

这样「重做闭环」这一环**完全零模型**，而且比任何识别方案都可靠。这是整个错题管理里最值钱的一个工程技巧。

### 6.2 输出形态：只出 A4 PDF 【已定 2026-09-02】

复习卷**只渲染 A4 PDF**，题目页与答案页分开。不做长图 PNG，不做屏幕逐题。

代价与应对：
- 家长群里没有打印机的家庭用不了完整闭环。**接受这个损失**，试用时直接问「你家能打印吗」，把没打印机的家庭标记出来单独看留存。
- 排版逻辑仍然抽象成「题目裁剪图 + 短码 + 分页」的布局函数，渲染后端隔离在一个模块里。**不是为了将来出长图，是为了改版式时不用动业务代码。**

### 6.3 遗留问题：去手写

题干截图里包含孩子上次写的错误答案。竞品（试卷宝、蜜蜂试卷）的核心卖点之一就是「去手写还原空白卷」。

**v1 定为：家长在裁剪时拖一个白色遮罩盖住答案区** 【已定 2026-09-02】——零模型，每题多 3 秒。

遮罩是 Problem 上的一个可选矩形字段（相对裁剪图的坐标），组卷渲染时才应用。原图和裁剪图都不被破坏，将来换成模型 inpainting 时直接忽略这个字段即可，不产生数据迁移。

已知差距：竞品（试卷宝、蜜蜂试卷）的「一键去手写还原空白卷」是自动的，v1 是手动的。真正的去手写需要模型 inpainting，属于 v2。

---

## 七、技术选型与部署

### 7.1 栈

单人业余，唯一标准是**减少要维护的东西**：

- **全栈一体**：Next.js（或同类）一个项目搞定前后端，不要拆微服务、不要单独的 BFF
- **数据库**：Postgres（Supabase / Neon 免费档足够 10–50 个家庭）
- **图片存储**：对象存储（R2 / 七牛）。**图片是成本和体积的大头**，从第一天就别存数据库里
- **前端**：移动端优先，图片框选/点选交互是唯一的技术难点，其余是表单和列表

### 7.2 给家长群试用的现实路径

想给 10 个家长试用，不需要 App、不需要小程序、不需要备案：

**做成手机浏览器能打开的 Web App，部署到静态托管，链接直接发群里。**

- 零审核、零应用商店、改完即时生效
- 微信里打开有一些限制（相机调用、下载文件），但拍照上传和查看是通的
- 缺点：国内访问境外托管会慢。10 个人的试用能忍；真要扩大再解决备案和国内部署

**明确不做**：不注册小程序（个人主体在教育类目下受限）、不发应用商店、不做 App 打包。这些都是验证完之后的事。

### 7.3 账号

v0.1 **不做账号**，单用户本地跑，先给自己用。
v0.2 给家长群时才加，用最简单的方案（手机号 + 验证码，或直接一人一个不可猜的链接）。别在验证阶段建用户体系。

---

## 八、MVP 切分

### v0.1 — 自己家先用（约 2–3 周）

跑通一条完整链路，哪怕每一步都是手动的：

1. 拍照 / 从相册选图
2. 手动框选错题（或点选，取决于第五章那个晚上的测试结果）
3. 填正确答案
4. 错题列表
5. 手动点「生成复习卷」→ 出 PDF/长图，带短码和二维码
6. 扫码回收 → 按序点对错 → 更新掌握状态

**验收标准不是功能齐全，是：你自己愿意连续用两周。** 如果你自己第五天就懒得打开，就别发给家长群。

### v0.2 — 家长群试用（约 +2–3 周）

7. 简单账号 + 多孩子
8. 复习调度真正跑起来（到期提醒）
9. 首页「今晚要做什么」
10. 家长周报

### v0.3+ — 接入模型

按 benchmark 结果，把输入层从 L0 升到 L1 或 L2。**此时下游一行代码都不用改**——如果第二章那条接口设计对了的话。

---

## 九、已知会踩的坑

1. **手机上的图片框选交互**：唯一真正难的前端工作，别低估。先做交互原型再设计数据库。
2. **把掌握状态做成计数器**：见 3.2，后果是规则一改历史数据全废。
3. **v1 就上知识点标注**：会立刻把录入摩擦拉到竞品同等水平，正是要避开的东西。
4. **复习卷没题可组**：孩子错题不够时卷子只有 2 道题，打印不划算。需要一个「凑数策略」——放宽到期日、或混入已掌握题抽查。**这个边界情况在自己用的第一周就会遇到。**
5. **图片体积**：一页作业原图 3–5MB，一个孩子一学期能到几个 GB。上传前压缩、裁剪后只存 crop、原图设过期策略。
6. **孩子把复习卷做丢了**：真实会发生。复习卷要能重新生成同样的一份（sheet 内容持久化，不是每次现算）。
7. **自己用着用着就开始加功能**：v0.1 的六步之外一律记进 backlog，不要动手。单人业余最大的风险不是做不出来，是**做不完**。
