# 模型准确率基准测试方案

> 版本：v1 · 2026-09-02
> 前置文档：《作业批改与错题管理App市场调研及可行性分析.md》
> 定位：单人业余、自家孩子作为首批用户的技术可行性验证。不涉及备案、商业化、获客。
> 预期投入：4 个晚上（或 1 个周末）+ 100 元以内模型调用费。

---

## 一、这个测试要回答的问题

**不是**「准确率是多少」。准确率是个没有决策价值的数字。

真正要回答的是：

> **在保证「几乎不冤枉孩子」的前提下，模型能替我自动判掉多少题？剩下的我自己看，还能不能比现在快？**

把它写成一个可测的量：

- 令 **coverage** = 系统自动判定的题目占比（其余标记为「不确定，请家长确认」）
- 令 **FPR**（假阳性率）= 在自动判定的题里，把**做对的题判成错**的比例

产出的核心结果是一条 **coverage–FPR 曲线**。产品的生死取决于这条曲线的形状，而不是某个单点准确率。

### 为什么是 FPR 而不是准确率

两类错误的代价完全不对称：

| 错误类型 | 现象 | 后果 |
|---|---|---|
| **FP 假阳性**（把对的判成错） | 孩子做对了，系统打叉 | **响的失败**。孩子会当场喊冤，家长立刻发现，信任瞬间归零。一次就够 |
| **FN 假阴性**（把错的判成对） | 孩子做错了，系统放过 | **哑的失败**。当晚没人发现，但这道错题永远进不了错题本，复习闭环有洞 |

FP 杀信任，FN 杀价值。**必须分开测、分开定门槛。**混在一个「准确率」里，等于把这两件事平均掉。

### 把原文档的模糊门槛翻译成可测数字

原文档写「每十页作业家长人工修正少于一次」。换算：

- 一页小学数学作业约 12 题，10 页 = 120 题
- 孩子正确率按 90% 算 → 其中约 **108 题是做对的**
- 「10 页只错一次」→ FP 次数 ≤ 1

> **FPR 必须 < 1/108 ≈ 0.9%**

同理，10 页里约 12 道真错题，漏判 1 道 → **FNR ≤ 8%**。

这两个数就是本次测试的及格线。注意 0.9% 是个很狠的要求——**这正是要先测的原因**。

---

## 二、指标定义

任务必须拆成三层分开测，否则「错了」这个结论没有可操作性——你不知道该换模型、改 prompt，还是加规则引擎。

### L1 检出（Detection）
一页 N 道题，模型找出几道？
- **漏检率** = 未被找到的真值题 / 总题数
- **虚检率** = 凭空多出来的题 / 预测题总数

漏检的题在下游是隐形的，它会伪装成「这页没错题」。**漏检率必须单独看**。

### L2 转写（Transcription）
在检出的题里，模型读对孩子写的答案了吗？
- **转写准确率** = 归一化后完全一致的题 / 已匹配题数
- 归一化规则：去空格、全半角统一、分数写法统一（`1/2` = `½`）、单位保留

这一层把「模型不认得铅笔字」和「模型不会算数」分开。小学数学的错误绝大多数在这一层，不在推理层。

### L3 判定（Verdict）
- **FPR** = 真值对 → 判错 的题数 / 真值对的总题数 ← **主指标**
- **FNR** = 真值错 → 判对 的题数 / 真值错的总题数 ← 次指标
- 空题、看不清的题单独归类，不计入以上两项

### L4 置信度校准（Calibration）← 最关键，也最容易被跳过
原调研文档的整个产品设计建立在「高/中/低置信度分级」上。**这个设计成不成立，就取决于这一层。**

要测的是：**模型说「我很确定」的时候，是不是真的确定？**

- 按置信度降序排列所有题，取前 k% 作为「自动判定」
- 对每个 k 计算该子集的 FPR，画出 **risk-coverage 曲线**
- 如果曲线是平的（高置信度桶里的 FPR 和整体一样），**说明置信度没有区分度，分级设计是假的**，整个产品方案要重做

置信度的两种来源都要测：
1. **模型自报**（让它输出 confidence 或 `unsure` 标记）——通常过度自信，但零成本
2. **自洽投票**（同一题跑 3 次，不一致即低置信）——更可靠，成本 ×3

---

## 三、数据集构造

### 3.1 来源：翻旧作业本

不要等新数据。家里一学期的旧数学作业本和试卷就是现成语料，**一个晚上能拍 40–60 页**，而且是真实分布（真实的字迹、涂改、光线、拍摄角度）。

### 3.2 规模

分两轮，别一次做满：

| 轮次 | 页数 | 约题数 | 目的 |
|---|---:|---:|---|
| **Round 0 快筛** | 20 页 | ~240 题 | 判断「有戏 / 没戏」，淘汰明显不行的模型和方法 |
| **Round 1 定档** | 补到 50 页 | ~600 题 | 对 Round 0 的赢家出可信的 coverage–FPR 曲线 |

**统计现实：** 600 题里，做对的约 540 题。要验证 FPR < 0.9%，意味着允许出现 ≤ 4 次 FP。这个样本量只够区分「1% 量级」和「5% 量级」，**不足以精确测量 0.5% 和 0.9% 的差别**。接受这一点——你要的是量级判断，不是论文级置信区间。真正的精确数字来自后面自己天天用时的线上统计。

### 3.3 分层：必须故意收集难样本

只拍工整清晰的作业，会得到一个虚高的漂亮分数，然后在真实使用第三天崩掉。

每个维度都要有样本（建议各占 10–20%）：

- **题型**：口算 / 竖式 / 脱式 / 填空 / 选择 / 判断 / 应用题（多步）/ 单位换算 / 图形题
- **书写**：工整 / 潦草 / 有涂改 / 有橡皮擦痕 / 铅笔太淡
- **拍摄**：正对 / 有透视倾斜 / 局部阴影 / 反光 / 装订线弯曲 / 题目跨页
- **答案形态**：整数 / 小数 / 分数 / 带单位 / 多空一题 / 过程与答案分离

### 3.4 标注格式

一页一条 JSON Lines。你本来就要批改，顺手记录，**边批边标**。

```jsonl
{"page_id":"p001","img":"images/p001.jpg","meta":{"grade":4,"type":["竖式","应用题"],"quality":"涂改+倾斜"},"items":[{"idx":1,"stem":"37+45=","child":"82","truth":"82","verdict":"right"},{"idx":2,"stem":"6×7=","child":"49","truth":"42","verdict":"wrong"},{"idx":3,"stem":"...","child":"","truth":"15","verdict":"blank"}]}
```

`verdict` 取值：`right` / `wrong` / `blank` / `unclear`（连你自己都看不清孩子写的什么——这类题模型也不该被要求判对，单独统计）

**降低标注摩擦的技巧**：先让一个模型跑一遍生成草稿 jsonl，你只做**修正**而不是从零录入。注意别被草稿带偏——凡是你改动过的题，标个 `reviewed: true`。

---

## 四、实验矩阵

### 4.1 方法 arm（这是本次测试真正的重点）

比「哪个模型强」重要得多的是「用什么架构调模型」：

| Arm | 做法 | 验证什么 |
|---|---|---|
| **A0 朴素基线** | 整页一次调用，直接输出每题判定 | 天花板参考，也是最省事的实现 |
| **A1 两阶段** | 第一步只做检出+转写（不判对错），第二步逐题判定 | 把 OCR 错误和推理错误**分离**，定位瓶颈在哪层 |
| **A2 混合判题** ⭐ | A1 的转写结果 + **确定性求解**：口算/竖式/脱式用 Python 直接算，只有应用题才交给模型判 | 把「模型判错」降级成「模型认错字」。小学数学大部分题是**可被程序精确求解**的，让模型判对错是浪费且不可靠 |
| **A3 自洽投票** | 在 A2 基础上跑 3 次，结果不一致 → 自动标为低置信 | 最便宜可靠的置信度来源 |

**我的预测：A2 会明显赢，而且不是小幅度。** 这也验证了原调研文档 8.2 提出的「混合判题」策略。如果 A2 没赢，那是个很有价值的意外，值得深挖原因。

### 4.2 模型分组

| 组 | 模型 | 用途 |
|---|---|---|
| **上限组** | `claude-opus-5`（$5/$25 每百万 token，1M 上下文）、`claude-sonnet-5`（$2/$10） | **测天花板**：如果最强的模型都到不了 FPR<1%，那不是模型选型问题，是任务本身的问题，直接止损 |
| **可上线组** | 通义千问 VL（阿里百炼）、豆包视觉（火山方舟）、GLM-4V（智谱）、阶跃 Step | 未来若开放给别人用，未成年人作业图片不能出境，**只能用国内可商用模型**。具体模型 ID 去各家控制台查当前版本，别照抄网上的旧 ID |
| **成本对照** | `claude-haiku-4-5`（$1/$5） | 看便宜模型在 A2 架构下够不够用（因为 A2 把难的部分交给了 Python） |

**现阶段（自用）用哪个都行；但要评估"能不能做成产品"，上限组和可上线组的差距必须测出来。** 这个差距如果很大，产品结论就完全不同。

### 4.3 调用注意事项

- **用 Batch API 跑**：基准测试没有延迟要求，`client.messages.batches.create` 是 **50% 价格**。结果乱序返回，**按 `custom_id` 取，绝不能按位置取**。
- **图片走 Files API**：同一张图在多个 arm、多次采样中反复用，上传一次拿 `file_id` 复用。
- **A3 的多次采样**：Opus 5 / Sonnet 5 不支持 `temperature` 参数（会返回 400）。变异性靠 prompt 顺序扰动或 effort 档位差异获得，不要去传 temperature。
- **结构化输出**：用 `output_config.format` 约束返回 schema，别用正则去抠模型的自然语言输出——那会引入一层与被测能力无关的解析错误，污染指标。

### 4.4 成本估算

一页作业图约 2000–2500 输入 token。

Round 0：20 页 × 4 arms × 平均 1.5 次采样 ≈ **120 次调用**
Round 1：50 页 × 2 arms × 2 模型 × 1.5 ≈ **300 次调用**

Opus 5 全量走 Batch：约 **¥50–80**。国内模型更便宜。

> **成本不是这个项目的约束条件，别为省几十块把样本量砍到没有统计意义。**

---

## 五、判据：什么分数算过

测完，看 coverage–FPR 曲线，对号入座：

### 🟢 绿灯
**coverage ≥ 70% 时，FPR ≤ 0.9%**

原方案成立。可以按「自动批改 + 家长确认少数」的产品形态往下做。每页 12 题，你只需要看 3–4 道，这确实比自己全批快。

### 🟡 黄灯
**要把 FPR 压到 0.9% 以下，coverage 只能到 40–70%**

产品定位必须改：不是「自动批改」，是「**帮你排序的批改助手**」——它把最可能有问题的题排在前面，你还是要过一遍，但顺序被优化了。价值依然真实（家长的时间花在刀刃上），但**承诺必须诚实**，不能宣称自动。

### 🔴 红灯
**无论 coverage 怎么切，FPR 都下不到 2%**

放弃自动批改。转做「**错题录入 + 复习闭环**」工具：家长自己判对错（本来就在做），系统只负责**圈题、去手写、结构化存储、按间隔生成复习卷**。

**这条路依然有价值**——原文档第五章总结的用户抱怨里，「录入步骤多、排版乱、收藏了不复习」全部属于这一层，且**完全不依赖模型准确率**。红灯不是项目死刑，是范围收缩。

### 另外三个必须一起看的数字

1. **L4 曲线是不是平的** → 平的话，「置信度分级」这个产品核心设计不成立，无论 FPR 多少都要重新设计交互
2. **漏检率** → 如果模型经常整题看不见，用户会以为「这页全对」，这是比 FN 更隐蔽的失败
3. **A2 相对 A0 的提升幅度** → 决定要不要投入写确定性求解器（这是后续最大的一块工程量）

---

## 六、四个晚上的执行排期

| 晚上 | 产出 | 时长 |
|---|---|---|
| **① 采数** | 翻旧作业本，拍 20 页（Round 0）。按 3.3 的分层检查一遍，缺哪类补哪类 | 1.5h |
| **② 标注** | 先跑一遍模型生成草稿 jsonl，再逐题修正。240 题 | 2h |
| **③ 建 harness** | 目录结构 + 调用封装 + 匹配算法 + 指标计算。跑通 A0 和 A2 | 3h |
| **④ 出结果** | 补跑 A1/A3，画 coverage–FPR 曲线，对照第五章判据下结论 | 1.5h |

**建议顺序上的一个反直觉点**：晚上 ③ 里，**最难的不是调模型，是「把预测题和真值题对上」**（模型的分题边界和你的编号不一致时怎么匹配）。先用「按顺序 + 题干文本模糊匹配」的朴素方案跑通，不要一开始就搞 IoU 框匹配。

### 目录结构

```
bench/
  images/           # 原图
  truth.jsonl       # 人工标注
  runs/             # 每次实验的原始输出（按 {model}_{arm}_{时间戳} 存）
  src/
    call.py         # 模型调用封装（batch 提交 + 结果回收）
    match.py        # 预测题 ↔ 真值题 匹配
    metrics.py      # L1-L4 指标计算
    solve.py        # A2 的确定性求解器（表达式解析 + 计算）
  report/           # 曲线图和结果表
```

---

## 七、明确不做的事

这一轮**不碰**，列在这里是为了防止自己跑偏：

- ❌ 不写任何 UI / App / 小程序
- ❌ 不做复习算法、组卷、导出 PDF
- ❌ 不碰语文、英语，不碰听力
- ❌ 不做账号、不做多用户、不考虑备案
- ❌ 不为了「跑通」而挑好拍的作业——那是自欺，且会让后面所有决策建立在假数据上
- ❌ 不追求 harness 的工程优雅度，它是一次性的测量工具

**一句话：这一轮的唯一交付物是一条曲线和一个 go/no-go 结论。**
