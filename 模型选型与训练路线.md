# 模型选型与训练路线

> v1 · 2026-09-03
> 前置：《模型准确率基准测试方案》定了批改怎么测，《切题检出测试方案》定了检出怎么测。本文档定**用什么模型**、**要不要自己训**。

---

## 〇、术语

| 术语 | 含义 |
|---|---|
| **VLM**（视觉语言模型） | 能同时吃图片和文字、输出文字的大模型。Kimi、Qwen-VL、GLM-4V 都是。通用能力强，但干一件小事也要跑整个大模型。 |
| **版面分析**（layout detection） | 一类**专用**视觉模型，只干一件事：在文档图片上框出「标题/段落/表格/公式/图」这些块。不理解内容，只认版式。典型是 YOLO 系列的检测模型，CPU 上 0.3–1 秒，GPU 上几十毫秒。 |
| **OCR** | 把图片里的文字转成字符串。**不判对错**，只认字。手写体和数学公式是 OCR 里最难的两块。 |
| **mAP** | 目标检测的标准指标，衡量框的位置准不准。检出任务里我们不用它——见《切题检出测试方案》，我们用「错题召回率 + 虚检数」，因为家长在意的是「有没有漏题」，不是「框差了 5 像素」。 |
| **SFT**（监督微调） | 拿「输入→期望输出」的成对数据接着训一个已有模型。需要几千到几万条标注。 |
| **LoRA** | 一种省钱的微调方式：冻住原模型，只训一小组新增参数。显存和时间要求降一个数量级，效果接近全量微调。 |
| **蒸馏** | 用大模型的输出当标注去训小模型。这里的用法：用慢而准的 VLM 给几千张作业标框，再训一个快的小检测模型。 |
| **RPM** | 每分钟请求数上限。当前 Kimi 账号是 **3**。 |

---

## 一、两个任务，别用一个模型硬扛

| 任务 | 要什么能力 | 难度 | 当前状态 |
|---|---|---|---|
| **检出（切题）** | 认版式：这页有几道题，各自在哪 | **低**。不需要读懂题目 | ✅ 已上线 |
| **批改（判对错）** | 读手写 + 理解题意 + 算 + 比对 | **高**。是产品的天花板 | ❌ 未开始 |

这两件事被一个 VLM 一起干是当前的偷懒实现，不是正确架构。分开的三个理由：

1. 检出用不到 VLM 的推理能力，却付了推理的时间和钱（实测 12 秒）。
2. 检出可以本地跑，**图片不出服务器**，未成年人数据合规问题直接消失。
3. 批改的准确率是产品命脉，必须能单独调阈值、单独评测、单独换模型。混在一起两个都调不动。

《系统架构》§二那条 `Attempt` 稳定接口就是为这个准备的——换哪一层，下游都不动。

---

## 二、检出（切题）选型

### 2.1 现状实测

同一张 1600×2200 合成作业页（12 道题：6 口算 + 3 竖式 + 3 应用题），把模型输出的框画回图上人工看：

| 模型 | 检出 | 耗时 | 结论 |
|---|---|---:|---|
| `kimi-k3` | 12/12，0 漏 0 虚 | 125–146s | 推理模型，输出 6000+ token 思考。不可用 |
| `kimi-k2.6` | 12/12 | 136s | 同上 |
| **`kimi-k2.7-code-highspeed`** | **12/12** | **9–12s** | 三次运行稳定，**当前采用** |

坑：这些模型**不接受 `temperature` 参数**，传任何非 1 的值直接 400。

### 2.2 这个结果不能当结论

合成图是**最容易的输入**：正射、无阴影、印刷体、纯白底、题间留白规整。真实照片有透视倾斜、手指入镜、褶皱阴影、铅笔淡痕、上一题的字侵入下一题的空白区。

我自己在《切题检出测试方案》里写过「不为了跑通去挑版式规整的作业，那是自欺」——现在正好踩了这条。**12/12 在拿到真实照片复测之前，不作数。**

### 2.3 三条问题

| 问题 | 严重度 | 说明 |
|---|---|---|
| RPM 3 | 🔴 给家长群试用时 | 3 个家长同时用就排队。已加 429 退避重试，但那是缓解不是解决 |
| 12 秒 | 🟡 | 已用后台异步规避，用户感知不到。但省不掉手动圈的价值也就打了折 |
| 图片出境到第三方 | 🟡 | Kimi 是国内厂商，不构成数据出境。但仍是把未成年人作业图传给第三方 |

### 2.4 候选方案

| 方案 | 代表 | 耗时 | 成本 | 图片外传 | 判断 |
|---|---|---|---|---|---|
| **通用 VLM API**（现状） | Kimi / Qwen-VL / 豆包 / GLM-4V / 混元 | 9–12s | 按 token | 是 | 当前可用，有 RPM 天花板 |
| **专用版面分析模型（本地）** | DocLayout-YOLO、PaddleOCR PP-Structure | **0.3–1s（CPU）** | **0** | **否** | **应该测** |
| **自训检测模型** | YOLO 系列微调 | 同上 | 训练一次性 | 否 | 有数据之后 |

版面分析模型是为「文档图片切块」这件事专门做的，参数量比 VLM 小三四个数量级。它认的是**版式**不是内容，而作业页恰好版式规整——题号 + 题干 + 作答区，这正是它最擅长的形态。

**已知不确定性**：这类模型的训练数据是论文、财报、扫描书籍，**没见过小学作业本**。开箱效果可能很差，尤其是手写和格子本。所以是「应该测」，不是「应该换」。

### 2.5 行动

| 顺序 | 事项 | 工作量 | 判据 |
|---|---|---|---|
| 1 | **拍 20 张真实作业照片**，跑现有 Kimi 检出 | 1 小时 | 错题召回率 ≥ 90% 且平均虚检 ≤ 2/页 → 保持现状 |
| 2 | 同一批图跑 DocLayout-YOLO / PP-Structure 对照 | 半天 | 召回率差距 < 5% → 换本地，RPM 和外传问题一起消失 |
| 3 | 若两者都不行 → 退回 L0.5 | 1 天 | 见《系统架构》§五：点一下 + 两个滑块 |

第 1 步是**任何其他决定的前置**。现在所有关于检出的判断都建立在一张合成图上。

---

## 三、批改（判对错）选型

### 3.1 这是产品的天花板

《商业化瓶颈》§二的结论：手动录入的产品差自动批改的产品**两个数量级**的用户规模。要越过去，必须做到 L2。而 L2 的门槛是一个具体数字：**FPR < 0.9%**（推导见《模型准确率基准测试方案》）。

### 3.2 按题型分层，难度差三个量级

不要问「能不能批改小学数学」，要按题型问：

| 题型 | 要什么 | 难度 | 备注 |
|---|---|---|---|
| **口算 / 计算题** | 认手写数字 + 算一遍 | 🟢 低 | 答案唯一可验证，**先只做这个** |
| **竖式** | 认多行手写 + 理解对位 | 🟡 中 | 过程错但答案对，算不算错？产品问题不是模型问题 |
| **填空 / 单位换算** | 认手写 + 知道标准形式 | 🟡 中 | 「1.5 米」vs「150 厘米」等价判定 |
| **应用题** | 读题 + 理解 + 列式 + 算 + 判过程 | 🔴 高 | 答案对而列式错，很常见 |
| **几何 / 作图** | 视觉推理 | 🔴 高 | 不做 |

**决定：L2 第一版只做口算题**，其余保持 L1（家长点选）。理由：口算题占小学 3–4 年级作业量的大头、答案唯一、错了能明确说出错在哪，而且**判错的代价最低**（家长一眼能看出系统判错了）。

### 3.3 关键设计：三步而不是一步

让 VLM 直接「看图说这题对不对」是最差的用法——它会把认字、计算、比对三件事混在一次生成里，错了不知道错在哪，也没法单独调。

```
① 认字：手写答案 → 字符串        ← OCR 任务，可单独评测
② 算：题干 → 正确答案            ← 可用代码验证，不需要模型
③ 比对：①==② ?                  ← 纯逻辑
```

好处：② 可以用**代码算**（口算题的题干解析出来就是个表达式），完全不会错；错误来源被压缩到只剩 ①；每一步都能单独测准确率。

这也顺带解决《痛点与针对性设计》§2.5 的「模型算答案」——同一套 ①② 复用。

### 3.4 候选

| 类型 | 代表 | 适合 |
|---|---|---|
| 国内通用 VLM API | Kimi / Qwen-VL / 豆包 / GLM-4V / 混元 | 三步都能干，先用它拿基线 |
| 专用手写 OCR | 腾讯云 / 阿里云的教育 OCR 接口 | 只干 ①，可能比 VLM 准 |
| 开源数学 OCR | PaddleOCR 公式识别、UniMERNet | 印刷公式强，**手写弱** |

**不用**：Mathpix（境外）、任何境外模型（约束见《商业化瓶颈》§四）。

### 3.5 行动

必须在写代码之前先建评测集。顺序：

1. 攒 **200 道口算题**的真机样本（图 + 手写答案 + 人工标的对错），这是基线
2. 拿 2–3 个国内 VLM 跑三步流程，出 coverage–FPR 曲线
3. 曲线达不到 FPR < 0.9% → L2 不做，产品停在 L1，重新评估

第 3 条是真实可能的结果，不是走过场。

---

## 四、模型训练：现在不该训

### 4.1 三条理由

| 理由 | 说明 |
|---|---|
| **没有数据** | 训检出至少要几百张标注过的真实作业页。现在有 **0 张**（合成图不算） |
| **没有基线** | 不知道现成 API 差多少，就不知道训练要补多少。可能根本不用训 |
| **不是瓶颈** | 当前瓶颈是《痛点与针对性设计》§二的必填答案，那是半天的前端改动，收益比训模型大得多 |

**结论：这个阶段训模型是把时间花在最不缺的地方。**

### 4.2 但现在就该攒数据

这是唯一「现在做成本近零、以后做成本极高」的一件事，和《商业化瓶颈》§五的补字段是同一个论点。

家长每一次操作都是一条免费标注：

| 家长动作 | 等价标注 | 用于训什么 |
|---|---|---|
| 采纳模型给的框 | 检出正样本 | 检出 |
| 手画模型没给的框 | **漏检样本**（最值钱） | 检出 |
| 拖动模型给的框 | 框回归修正 | 检出 |
| 无视模型给的框 | 虚检样本 | 检出 |
| 填正确答案 | 题干→答案 对 | 批改 ② |
| 回收时点对/错 | 手写答案→判定 | 批改 ①③ |
| 点「判错了」申诉 | **硬负样本**（最值钱） | 批改 |

现在这些**全部在丢**。落库改动见《痛点与针对性设计》§三，2 小时的活。

### 4.3 什么时候训、训什么

| 触发条件 | 训什么 | 方法 | 代价 |
|---|---|---|---|
| 攒够 300–500 张标注作业页，且现成方案召回率 < 90% | 检出模型 | YOLO 微调 | 单卡几小时，**便宜** |
| 检出够用但太慢 / 撞 RPM | 检出模型 | **蒸馏**：用 Kimi 给几千张标框，训小 YOLO | 单卡几小时 |
| 攒够 3000+ 条判定样本，且 API 的 FPR 下不来 | 批改 OCR（步骤①） | LoRA 微调 | 数天，贵 |
| —— | 批改的推理部分 | **不训**。用代码算（§3.3 ②） | 0 |

**优先级很清楚：检出的微调便宜且大概率有效，批改的微调昂贵且不确定。真要训，从检出开始。**

### 4.4 不训的东西

| 不训 | 用什么代替 |
|---|---|
| 从零训基础模型 | 没有任何理由 |
| 训「讲解」模型 | prompt + 年级约束（《痛点与针对性设计》§六 P8） |
| 训计算能力 | 代码算，模型不参与计算 |

---

## 五、评测集：比模型更重要的资产

模型可以换，评测集换不了。三份，都要**真实数据**：

| 评测集 | 规模 | 内容 | 来源 |
|---|---|---|---|
| **检出集** | 20 → 100 页 | 真实作业照片 + 人工框的题框 | 自己拍 + 家长群 |
| **批改集**（口算） | 200 → 1000 题 | 题图 + 手写答案 + 人工判定 | 同上 |
| **硬负样本集** | 持续增长 | 模型判错、被家长申诉推翻的 | `correction` 表自动积累 |

第三份是最有价值的，而且**只要把《痛点与针对性设计》§4.3 的申诉入口建起来，它会自己长**。

配套：`scripts/export-dataset.mjs`，把库里的数据导成 YOLO/COCO 格式。攒够了直接能训，不用回头考古。

---

## 六、路线图

| 阶段 | 输入层 | 检出 | 批改 | 前置条件 |
|---|---|---|---|---|
| **现在** | L1 | Kimi VLM API，12s | 无 | —— |
| **下一步** | L1 | 同上 + **真实照片复测** | 无 | 拍 20 张 |
| **v0.2** | L1 | 本地版面模型（若对照测试通过） | 无 | §2.5 第 2 步 |
| **v0.3** | L2（仅口算） | 同上 | 国内 VLM，三步流程 | 批改评测集 200 题 + FPR < 0.9% |
| **以后** | L2 | 自训/蒸馏（若需要） | LoRA（若需要） | 数据攒够 + 证明 API 不够用 |

每一格的前置条件都是硬的。**跳过前置条件往下走，等于把「能不能做」当成「已经做到」。**

---

## 七、约束（不可协商）

| 约束 | 出处 |
|---|---|
| 不使用境外模型（Anthropic / OpenAI / Mathpix 等） | 用户明确要求 + 数据出境合规 |
| API key 只在服务端环境变量，不进前端、不进仓库 | —— |
| 家长群试用开始即处理他人未成年人信息，模型必须国内 | 《商业化瓶颈》§四 |
| 检出失败必须静默降级为手动，不阻塞主闭环 | 已实现 |
