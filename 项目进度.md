# 项目进度

> **每次改动后更新这里。** 这是项目唯一的状态入口。
> 最后更新：2026-09-03
> **交接说明：后续按 `TRD-错题本v0.1.md` 开发。该文档已按实际代码全面回填**，
> 与初版设计不一致的地方都标注了「实际」与原因，§11 是接手后的开发项清单。

- 线上：**https://www.twincle.com.cn/reborn**
- 仓库：https://github.com/ookami-change/reborn
- 当前分支：`feat/mark-canvas`（与 `master` 同步）

---

## 一、一句话状态

**v0.1 全部功能已上线可用，主闭环从未被真人完整走过一遍。** 输入层处于 L1（自动切题 + 家长点选），批改模型未接入。

---

## 二、v0.1 开发项（TRD §10）

| 序 | 内容 | 状态 | 提交 |
|---|---|---|---|
| 1 | 圈题页框选交互原型 | ✅ | `0e0bbef` |
| 2 | 建库建表、对象存储、上传链路 | ✅ | `6b29266` `f57f8eb` |
| 3 | 圈题 Step2（遮罩、答案）+ 裁剪落库 | ✅ | `eb3ca68` |
| 4 | 错题本列表、详情页 | ✅ | `f708464` |
| 5 | Leitner 状态机 + 首页聚合 | ✅ | `f708464` |
| 6 | 组卷 + PDF 渲染 + 二维码 | ✅ | `9bb5aca` |
| 7 | 扫码回收 + 状态更新 | ✅ | `9bb5aca` |
| 8 | **真机联调** | 🔴 **未做** | —— |
| 9 | 接入自动检出 | ✅ | `9726103` |

部署：`6687c0a`。腾讯云轻量服务器，Docker + Caddy 挂 `/reborn` 子路径，与同机的 a-share / baby-planet 共存。

---

## 三、阻塞项（需要用户）

| # | 事项 | 为什么阻塞 |
|---|---|---|
| B1 | **用手机走一遍完整闭环** | 进行中。已走到：拍作业✓、圈错题✓、错题详情✓。走查中修复两个导航 bug（待整理入口指错、复习卷入口被到期数卡死）。排版已由我本地渲染 PNG 自查并修掉三处（见§五 09-03）。**仍需真人验证的只剩两件**：①打印到纸上作答区 12mm 够不够写 ②手机扫二维码能不能跳转 |
| B2 | **拍 20 张真实作业照片** | 现在所有检出结论都建立在**一张合成图**上。没有真实照片，§四的所有模型决策都是悬空的 |
| B3 | 外网可达性确认 | 我的沙箱到 124.223.185.175:80 超时（22 端口正常），服务器自己访问自己公网 IP 返回 200。需要你从手机确认能打开 |

---

## 四、待办（按优先级）

优先级判据只有一条：**会不会导致家长在两周内停用**。

| # | 事项 | 工作量 | 出处 | 状态 |
|---|---|---|---|---|
| ~~T1~~ | ~~正确答案改可选，圈完即存；回收时行内补录~~ | 半天 | 痛点§二 | ✅ `43aa95e` |
| ~~T2~~ | ~~落库 `box_origin` / `detected_boxes`~~ | 2 小时 | 痛点§三 | ✅ `887b510` |
| T3 | 真实照片跑检出复测 | 1 小时 | 模型§2.5 | 依赖 B2 |
| T4 | 本地版面模型对照测试 | 半天 | 模型§2.5 | 依赖 T3 |
| T5 | 框住纸上答案区（零打字补答案） | 半天 | 痛点§2.5 | 待做，先看 T1 之后家长实际怎么用 |
| ~~T9~~ | ~~加鉴权（单口令）~~ | 1 天 | 本文档§六 | ✅ `ebfaa52` |
| ~~T9b~~ | ~~按家庭隔离数据~~ | 1–2 天 | —— | ✅ magic link + 查询作用域，7 接口越权实测全 404 |
| ~~T11~~ | ~~上 TLS~~ | 半天 | 本文档§六 | ✅ Let's Encrypt 自动签发+续期，cookie 已带 Secure |
| ~~T10~~ | ~~修 MarkCanvas 的 setState-in-effect lint error~~ | 1 小时 | —— | ✅ 已随 `6403862` 消失 |
| T6 | `correction` 表 + 判定可追溯 | 1 天 | 痛点§四 | 接批改模型**前**必须做完 |
| ~~T7~~ | ~~合规字段~~ | 半天 | 瓶颈§五 | ✅ 列已加、软删除已接 |
| ~~T12~~ | ~~监护人同意 UI~~ | 半天 | 瓶颈§五 | ✅ 未同意时页面 302、接口 403 |
| T13 | 原图到期清理任务 | 半天 | 瓶颈§五 | 不急 |
| T8 | 假掌握处理 | —— | 痛点§5.2 | **暂不做**，攒两个月数据再定 |

T1、T2、T9、T9b、T7、T12 已完成。**家长群试用的代码侧已经没有阻塞项了。**

**域名和 HTTPS 已经解决**（`https://www.twincle.com.cn/reborn`）。剩下两件在你：

1. 拍 20 张真实作业照片（T3 依赖）
2. 打印一张复习卷 + 手机扫码（B1 依赖）——**现在扫码测试才有意义**，
   之前是裸 IP，微信会拦，测不出真实结果

这两件做完就可以生成邀请链接发群了（`deploy/invite.sh add "小明家"`）。

---

## 五、已定决策

| 日期 | 决策 | 理由 |
|---|---|---|
| 09-02 | 输出只做 A4 PDF，不做热敏小票 | 架构§6.2 |
| 09-02 | 去手写用手动白色遮罩，不做图像修复 | 架构§6.3 |
| 09-02 | 用 Leitner 5 档，不用 SM-2 / FSRS | 架构§4.1。参数可解释，家长能理解 |
| 09-02 | Problem / Attempt / MistakeCard 拆三张表 | 架构§3.2。掌握状态必须能由 attempt 序列重算 |
| 09-02 | 复习卷印二维码回收 | 架构§6.1。把「录结果」变成卷子上的物理入口 |
| 09-02 | 自建 Postgres 容器，不买 TencentDB | 218.4 元/月 vs 0 |
| 09-03 | **不使用境外模型** | 用户要求 + 未成年人数据出境 |
| 09-03 | 检出用 `kimi-k2.7-code-highspeed` | 12/12 且 9–12s；k3/k2.6 同样全中但 125–146s |
| 09-03 | 检出走**后台异步**，不阻塞圈题 | 12s 超 PRD 的 5s 要求，异步后用户无感 |
| 09-03 | PDF 中文按**字形轮廓**绘制，不内嵌字体 | pdf-lib 子集化乱码；不子集化 PDF 涨到 7MB。轮廓法 48KB |
| 09-03 | L2 第一版**只做口算题** | 模型§3.2。答案唯一、判错代价最低 |
| 09-03 | **现在不训模型，但现在就攒数据** | 模型§四。没数据没基线，且不是当前瓶颈 |
| 09-03 | 正确答案改可选，录入挪到扫码回收 | 该字段 20 处引用全是展示，不参与任何逻辑判断。回收时家长手里有原卷和孩子重做的，本来就在逐题看，补录边际成本接近零 |
| 09-03 | 作答区高度加下限 34pt / 上限 170pt | 按题图高度成比例是错的——孩子字高固定。口算行原本只有 7.2mm 写不下，竖式却给到 46.9mm 浪费 |
| 09-03 | 二维码 50→64pt，并在页眉印出回收链接 | 50pt 时单模块 0.53mm 正好卡在家用打印机可靠阈值上；扫码是闭环硬依赖，扫不出来家长完全无法录结果，必须有文字退路 |
| 09-03 | 排版断言直接测 `answerHeight` 而非反解像素 | 像素启发式太脆，量出过负数。出错的是几何函数，就测几何函数 |
| 09-03 | 鉴权先做单口令，不建 account 表 | 只有一个用户时，一张只有一行、密码还在环境变量里的 account 表是"看起来准备好了其实没有"的半成品。按家庭隔离是 T9b |
| 09-03 | 会话有效期 30 天 | 扫码回收发生在手机上，每次都要登录会毁掉「扫码即用」这个关键设计（架构§6.1） |
| 09-03 | 限流按 XFF **最右**值分桶 | 最左是客户端自己填的，可伪造；最右是 Caddy 追加的真实对端地址 |
| 09-03 | 已跑过的检出结果直接复用，不重跑 | 一次 12 秒且占一个 RPM 额度（上限 3）。`capture.detected_boxes` 本来就存着 |
| 09-03 | PRD/TRD 按实际代码全面回填 | 要交接给其他开发，文档与代码不符比没有文档更糟 |
| 09-04 | 隔离用 **magic link** 而非手机号/密码账号 | 零注册摩擦、不用短信服务、天然隔离。代价是链接转发即泄露，5–10 家庭可接受 |
| 09-04 | 越权一律返回 **404 而非 403** | 403 会泄漏「这个 id 存在，只是不属于你」 |
| 09-04 | 短码唯一性改为 `(child_id, short_code)` | 全局唯一在多账号下会撞键——每个孩子的第一张卷都叫 R01 |
| 09-04 | 删错题改**软删除** | 物理删会 cascade 掉 attempt，那是掌握状态可重算的唯一依据 |
| 09-04 | 同意版本放**签过名的 cookie**，不每次查库 | proxy 每个请求都跑，不能为它引入数据库往返。consent_log 才是记录本身 |
| 09-04 | 同意页如实写「照片会发给 Kimi」 | 不写就是隐瞒。空话式隐私承诺没有意义 |
| 09-05 | HTTPS 用 Caddy + Let's Encrypt，不买腾讯云 60 元/月的证书 | 加密强度和浏览器信任完全一样，都是 DV；付费多出的 OV/EV 需要企业主体，个人拿不到。Caddy 90 天自动续，付费证书一年后要手动重签 |
| 09-05 | cookie 的 `secure` 由 `BASE_URL` 推导，不硬编码 | 硬编码 true 会让本地 http://localhost 开发登不上 |
| 09-03 | 测试夹具 sample-homework 从 SVG 预渲染成位图 | SVG 用 Mac 字体，服务端 Linux 栅格化整页乱码。上传主链路不受影响（真实照片是位图） |

---

## 六、已知风险

| 风险 | 状态 |
|---|---|
| 检出的 12/12 是**合成图**跑出来的，真实照片未知 | 🔴 未验证，见 B2 |
| 打印到纸上作答区是否够写，只有算出来的 12mm，没有实物 | 🟡 见 B1 |
| Kimi 账号 **RPM 上限 3**，多家庭并发会排队 | 🟡 已加 429 退避重试 |
| 字体文件 7.9MB 被 gitignore，克隆后生成 PDF 会报文件不存在 | 🟢 README 已写明 |
| 密钥（腾讯云 ×2、Kimi ×1）曾在对话中明文出现 | 🟡 **建议轮换**。仓库和历史已扫过，无泄漏 |
| ~~任何人访问 URL 即可看到全部数据~~ | 🟢 已修 `ebfaa52`：全站单口令 |
| ~~共享口令，所有人看到同一批错题~~ | 🟢 已修：magic link 一家一条，7 接口越权实测全 404 |
| magic link 被转发出去 = 账号泄露 | 🟡 5–10 个家庭可接受，`invite.sh revoke` 可撤销 |
| **站点走 http，口令和 cookie 明文传输** | 🔴 挡得住误入的人，挡不住同网络嗅探。见 T11 |
| 数据模型缺合规字段 | 🟡 见 T7 |

---

## 七、文档地图

| 文档 | 回答什么问题 |
|---|---|
| `PRD-错题本v0.1.md` | 要做什么功能，验收标准是什么 |
| `TRD-错题本v0.1.md` | 怎么实现，表怎么建，接口怎么定 |
| `系统架构.md` | 为什么这么分层，Attempt 接口为什么必须稳定 |
| `痛点与针对性设计.md` | 用户会在哪里放弃，怎么改 |
| `模型选型与训练路线.md` | 用哪个模型，什么时候该自己训 |
| `模型准确率基准测试方案.md` | 批改准确率怎么测（coverage–FPR 曲线） |
| `切题检出测试方案.md` | 检出怎么测（错题召回率 + 虚检数） |
| `试用分发方案.md` | 发给家长群前必须读：域名/备案/HTTPS、微信里的坑、规模控制 |
| `商业化瓶颈.md` | 合规、获客、成本、单人业余的规模上限 |
| `作业批改与错题管理App市场调研及可行性分析.md` | 市场有多大，竞品什么样，用户骂什么 |

---

## 八、维护约定

- 改了代码 → 更新 §二 或 §四 的状态
- 做了取舍 → 记进 §五，写清理由（**理由比结论重要**，半年后只有理由能判断决策是否还成立）
- 发现新风险 → 记进 §六
- 完成阻塞项 → 从 §三 移走，并在 §五 记录验证结果
- 本仓库可能有多个 Claude 会话同时在改（2026-09-03 就发生过）。提交前用
  `git status` 确认改动范围，**不要无脑 `git add -A`**，会把别人半成品的
  工作扫进自己的提交
