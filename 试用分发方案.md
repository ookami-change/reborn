# 家长群试用分发方案

> 版本：v1 · 2026-09-04
> 目标：把当前 Web 应用交到 5–10 个家庭手上试用两周
> 相关：`项目进度.md` §三阻塞项、`商业化瓶颈.md` §三合规链条

---

## 〇、术语

| 术语 | 含义 |
|---|---|
| **ICP 备案** | 域名指向中国大陆服务器时必须做的工信部登记。分「非经营性」和「经营性」，**只要不向用户收费就是非经营性，个人身份可以办，免费**。 |
| **TLS / HTTPS** | 传输加密。没有它，登录口令和会话 cookie 在网络上是明文。 |
| **magic link** | 一次性登录链接。每个家庭一个长随机串的网址，点开即登录，不用注册也不用记密码。 |
| **数据出境** | 把境内收集的个人信息传到境外处理或存储。把数据库放在境外服务器也算。 |

---

## 一、结论

**继续用 Web，不做小程序，不做安卓包。**

但形态不是真正的问题。**当前拦路的是没有域名、没有备案、没有 HTTPS**——裸 IP 的 http 链接发进家长群，在微信里打不开，也没人敢点。

三个必须做的动作，按时间敏感度排序：

1. **今天就去买域名提备案**（这是唯一有 7–20 个工作日等待期的事）
2. 等待期内做完 T9b（按家庭隔离数据）、T7（合规字段）、B1/B2（真机走查、拍真实照片）
3. 备案通过后配 HTTPS（Caddy 自动申请证书，半天）

---

## 二、三个形态对比

| | 微信小程序 | **Web（推荐）** | 安卓 APK |
|---|---|---|---|
| 前端改造量 | **全部重写**。MarkCanvas 圈题画布的 canvas API 与 web 不同，要重做 | 0 | 0（webview 壳） |
| 能否绕开备案 | **不能**。小程序的网络请求只允许配置已备案的 https 域名，是硬性校验 | 不能 | **不能**。壳里访问的还是同一个境内后端 |
| 额外资质 | 个人主体小程序在教育类目下受限；代码审核 1–2 周，结果不确定 | 无 | 无 |
| iOS 覆盖 | ✅ | ✅ | ❌ **家长群里一半是 iPhone** |
| 安装摩擦 | 中（要搜索或扫码） | **最低**（点链接即用） | 高（要开"未知来源"，厂商强警告） |
| 信任成本 | 低 | 低（有域名+HTTPS 之后） | **极高**（让家长装来路不明的 apk） |
| 到可试用的时间 | 4–6 周 | **备案通过后半天** | 备案通过后 1 天，且只解决一半用户 |

**小程序**：改造量最大、审核结果不确定、备案一样躲不掉。对 5–10 个家庭的试用完全不成比例。等真要做微信传播（v1.0 之后）再说。

**安卓包**：唯一的理论收益是解决 PDF 下载问题，但直接放弃全部 iOS 用户，且要求家长安装未知来源的应用。收益为负。

---

## 三、看起来能跳过备案的捷径，为什么都不能走

| 捷径 | 为什么不行 |
|---|---|
| 继续用裸 IP `http://124.223.185.175/reborn` | ① 微信打开纯 IP 地址的链接会拦截或强提示风险 ② http 明文，口令 cookie 裸奔 ③ 腾讯云境内节点未备案的 web 服务随时可能被阻断 ④ **家长看到这种链接第一反应是诈骗——信任问题比技术问题更致命** |
| 部署到 Vercel / Cloudflare（境外），免备案免费 HTTPS | **把数据库放境外 = 未成年人个人信息出境**，比调用境外模型更严重。项目已明确"不使用境外模型"就是因为出境，把整个数据库搬过去自相矛盾。另外图片 4MB/张，国内访问慢 |
| 用别人已备案的域名挂靠 | 备案主体与实际运营者不符，出问题时责任在备案主体那一方 |

---

## 四、两条路径

### 路径 A：你名下已有备案过的域名 → 当天可上线

这是最快的。加一条子域名解析（如 `cuoti.你的域名.com`）指到 124.223.185.175，Caddy 配置改成域名并开自动 HTTPS。

> **先确认这一件事。** 同机还跑着 a-share / baby-planet，如果它们已经用域名对外，那备案已经有了，直接复用。

半天工作量。

### 路径 B：没有备案域名 → 7–20 个工作日

| 步 | 内容 | 耗时 | 费用 |
|---|---|---|---|
| 1 | 在腾讯云买域名（和服务器同一家，备案流程最省事） | 10 分钟 | ¥30–60/年 |
| 2 | 提交个人非经营性 ICP 备案 | 提交 1 小时，审核 **7–20 工作日** | 免费 |
| 3 | 备案通过，域名解析 + Caddy 开 HTTPS | 半天 | 0 |

**备案期间网站必须停止对外提供服务**，现在裸 IP 跑着严格说已经是违规状态。

### 备案的三个坑

| 坑 | 说明 |
|---|---|
| **网站名称不能带"教育""学习""培训"字样** | 这些属于前置审批类目，个人备案报了会被驳回。填「个人技术项目」「个人笔记工具」之类 |
| 需要人脸核验 | 腾讯云 App 上做，不用再去拍幕布 |
| 服务器必须是境内且在有效期内 | 腾讯云轻量满足 |

---

## 五、微信里的三个具体坑

### 5.1 PDF 打不开 / 存不下来（最要紧）

微信内置浏览器对 PDF 的处理很差：iOS 能预览但难保存，Android 经常下载后找不到文件。

**但这件事本来就不该在手机上解决——打印发生在电脑上。**

组卷结果页给三样东西：
1. 「复制链接」按钮 + 一行提示「发给微信『文件传输助手』，在电脑上打开打印」
2. 「下载 PDF」按钮（COS 直链，设 `Content-Disposition: attachment`）
3. 底部小字「打不开就点右上角 ⋯ → 在浏览器中打开」

### 5.2 扫码回收

备案域名下的二维码，微信扫码能正常跳转。**裸 IP 的二维码微信会拦。**
这是 B1 里"手机扫二维码能不能跳转"那一项——**在没有域名之前测不出真实结果**。

### 5.3 拍照上传

`<input type="file" accept="image/*">` 在微信内可用，能调起相机和相册。这一项没问题。

---

## 六、账号方案：一家一条 magic link

T9b 要做按家庭隔离，最省事的实现不是手机号+验证码（要短信服务，有资质和费用），而是：

- 每个家庭生成一条不可猜的长随机串链接，如 `/join/8f3a...c91`
- 点开即创建/绑定该家庭的 session，cookie 有效期 30 天

好处：零注册摩擦、天然数据隔离、不用短信、不用密码。
代价：链接被转发出去等于账号泄露。5–10 个家庭的试用可接受。

> 这也保住了「扫码即用」——扫二维码回收时不需要重新登录（`项目进度.md` §五 09-03 那条决策）。

### 6.1 怎么发进群：链接不能是同一条

**链接就是账号**——同一条链接被两个家庭点开，他们进的是同一个账号，会互相看到
对方孩子的错题，正是 T9b 要消掉的问题。所以每家必须一条不同的链接。

但「发家长群」和「每人一条不同链接」直接冲突：群里发 8 条链接，指望每人认领
属于自己那条，实际必然乱。三种做法：

| 做法 | 怎么发 | 问题 |
|---|---|---|
| A. 逐个私聊 | `invite.sh add "小明家"` 生成，一条条私聊 | 正确但麻烦，谁是谁得自己记 |
| **B. 群里发一条领取链接** | 点开自助开户，然后立刻引导他把应用装到手机桌面 | 转发到群外陌生人也能开户，需要设上限 |
| C. 群里发地址 + 私发邀请码 | 每人输一次码 | 多一步输入，摩擦最大 |

**选 B。** 它是唯一能真正「发群」的做法，A 保留给需要点名的人。

### 6.2 方案 B 的两个坑

**坑一：cookie 会丢，而丢了就找不回来。**
家长清了浏览器数据、换手机、或者在 iOS 上把网页加到桌面（**iOS 桌面应用有一套
独立于 Safari 的存储，拿不到 Safari 里的 cookie**），会话就没了。此时再点群里那条
领取链接，开出来的是一个**新的空账号**，之前的错题全找不回。

对策：领取后跳到 `/setup/<token>` 专属入口页，这一页
① 教他把应用加到手机桌面，② 把他的专属链接显示出来让他自己存好，
③ **从桌面图标启动时直接重走一遍 magic link**，所以登录状态永远不会丢。

**坑二：链接会被转发出去。**
三道门禁，逐层收窄损失：`CLAIM_LIMIT`（≤0 即整个功能关闭，默认就是 0）、
`CLAIM_CODE`（链接里的暗号，挡扫路径的爬虫）、名额上限（就算前两道破了损失也有界）。
谁开的户在 `invite.sh` 里带 `[群里自助领的]` 标记，多余的直接删。

### 6.3 加到手机桌面（安卓 / iOS）

比「收藏链接」好得多：有独立图标、点开是全屏窗口没有地址栏、不用再翻聊天记录找链接。
三个平台的操作完全不同，`/setup/<token>` 按 UA 分别给说明：

| 环境 | 怎么装 | 备注 |
|---|---|---|
| **微信内置浏览器** | **装不了**，先点右上角 ··· →「在浏览器打开」/「在Safari中打开」 | 家长十有八九是在微信里点开的，这一步不说清楚后面全白搭 |
| 安卓 Chrome | 有 `beforeinstallprompt` 时给「一键添加」按钮，否则教他走 ⋮ 菜单 | 要有 manifest + 注册了 fetch 的 service worker，否则只生成书签快捷方式而不是真正的应用 |
| iOS Safari | 底部分享按钮 →「添加到主屏幕」 | iOS 没有一键安装的接口，只能给图文步骤 |

技术要点：

- **每个家庭一份 manifest**（`/setup/<token>/manifest.webmanifest`），因为
  `start_url` 里要带上他自己的 magic link，从桌面图标启动才能直接登录进去。
  里面的地址一律用相对路径，这样线上的 `/reborn` 前缀和本地无前缀都不用特判。
- **service worker 刻意不做任何缓存**。它存在的唯一理由是满足 Chrome 的可安装判定，
  错题数据必须实时，离线缓存只会带来「改了没生效」这类难查的问题。
- `apple-mobile-web-app-capable` 要手写补上：Next 只发标准名 `mobile-web-app-capable`，
  那是 Safari 17.4（2024-03）才认的，旧 iPhone 只看 `apple-` 前缀那个。
- 老 iOS 不读 manifest 的 `start_url`，加到桌面的是当时那一页 `/setup/<token>`，
  所以这一页要自己判断「是不是从桌面启动的」并跳去 `/join/<token>`。

---

## 七、上线前必须完成的清单

| # | 事项 | 工作量 | 为什么卡在这 |
|---|---|---|---|
| **域名 + 备案** | 见 §四 | 7–20 工作日等待 | 没有它下面全都白做 |
| **T11 上 TLS** | 半天 | 备案后 | 现在口令和 cookie 明文 |
| **T9b 按家庭隔离** | 1–2 天 | 现在就能做 | **共享口令 = 所有家庭看到同一批错题，绝对不能发出去** |
| **T7 合规字段** | 半天 | 现在就能做 | `deleted_at` / `retention_until` / `consent_log`。现在加是加几列，以后加是数据迁移 |
| **B1 真机走完闭环** | —— | 现在就能做 | 主闭环从未被真人完整走过一遍 |
| **B2 拍 20 张真实作业** | —— | 现在就能做 | 检出的 12/12 是合成图跑的，真实照片表现未知 |

**T9b 是硬门槛。** 其余可以边试用边补，这一条不行。

---

## 八、时间线

```
今天        买域名，提交备案                    ← 唯一有等待期的事，先做
            ↓
等待期内     T9b 按家庭隔离      1–2 天
            T7  合规字段        半天
            B1  真机走完闭环
            B2  拍 20 张真实作业照片
            ↓
备案通过     域名解析 + Caddy 自动 HTTPS   半天
            生成 5–10 条 magic link
            ↓
            发家长群
```

若走路径 A（已有备案域名），把第一段换成半天配置，其余不变。

---

## 九、发进群里的形态

不要发裸链接。发一段图文：

- 一句话说清楚这是什么、要家长做什么（拍作业、圈错题、打印复习卷）
- 明说是**自己写的、免费、试用两周、随时可以删数据**
- 链接
- 一行「打不开就点右上角 ⋯ 在浏览器打开」

**试用期不要收钱。**收钱即触发经营性备案，会把整条合规链（企业主体 → 经营性许可 → 等保二级 3–8 万）提前拉起来。验证付费意愿用「到期问是否愿意续」，不要真实收款。

---

## 十、规模控制

**这一轮只发 5–10 个家庭，不要更多。** 三个真实约束：

| 约束 | 上限 |
|---|---|
| Kimi 账号 RPM 上限 3 | 多家庭并发检出会排队 |
| 单人业余的客服和误判核查 | 一个人扛不住几十个家庭的追问 |
| 个人非经营性备案下运营教育工具 | 灰色地带。不收费、不公开推广、规模小，现实风险很低；一旦公开传播性质就变了 |

最后一条如实说明：个人备案不得从事经营活动，不收费就不算经营；但「面向他人提供教育类服务」在教育移动应用备案的范畴内。**小范围熟人试用现实风险很低，公开推广就不是了。**
