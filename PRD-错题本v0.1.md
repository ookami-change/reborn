# PRD｜错题本 v0.1 产品需求文档

| 项 | 内容 |
|---|---|
| 版本 | v1.0 |
| 日期 | 2026-09-02 |
| 状态 | 待开发 |
| 配套文档 | 《TRD-错题本v0.1.md》 |

---

---

## 0. 实现状态（2026-09-03）

**FR-1…FR-8 全部已实现并上线。** 本文档已按实际回填，与初版不一致处已在对应条目标注。

主要变更：
- **FR-0 登录**：新增，初版没有鉴权
- **FR-2 Step 2 整个变为可选**，正确答案由必填改为选填，录入挪到 FR-6 扫码回收
- **FR-1** 改为单张上传（不是一次多张）
- **§6 版式**：作答区高度改为钳制，二维码放大到 64pt
- **§7 性能**：自动检出实测约 12 秒，超出 5 秒要求，改为后台异步规避

尚未验证：打印到纸上的作答区手感、手机扫码跳转、微信内置浏览器。

---

## 1. 产品定义

一个给小学 3–6 年级学生家长用的**数学错题闭环工具**。

家长拍下孩子做完的作业，在图上圈出做错的题，系统自动记录并排好复习计划；到期时一键生成可打印的 A4 错题卷；孩子重做后拍照或扫码录入结果，系统更新掌握状态。

**一句话承诺**：错题记一次，之后什么时候该重做、重做哪些题，系统替你安排。

---

## 2. 要解决的问题

家长每晚检查作业后面临三件事，现有产品都没有连成一条线：

| 问题 | 现状 |
|---|---|
| 错题记不下来 | 手抄费时；用 App 录入要逐题拍照、裁剪、选科目、打标签，步骤太多 |
| 记下来了不复习 | 大多数工具停在「收藏错题」，没有推动孩子定时重做 |
| 复习卷难生成 | 导出的题目字号不一、题型混排、答案没有独立成页，不能直接打印 |

v0.1 要验证的是：**把「圈错题 → 自动排复习 → 组卷打印 → 重做回收」这条链路做通，家长愿不愿意连续用两周。**

---

## 3. 用户

| 角色 | 说明 | 在产品里做什么 |
|---|---|---|
| **家长** | 主要用户和唯一操作者，小学 3–6 年级学生的父母 | 拍照、圈错题、填正确答案、生成复习卷、录入重做结果 |
| **孩子** | 不直接使用产品 | 在纸上做复习卷 |

v0.1 **不给孩子提供任何登录入口**。所有操作都在家长的手机上完成。

---

## 4. 核心流程

```
① 拍作业          家长拍下整页作业（可多页）
      ↓
② 圈错题          在图上圈出做错的题，逐题填正确答案、遮住旧答案
      ↓
③ 自动排复习      系统按 1/3/7/14/30 天间隔安排每道题的下次复习日
      ↓
④ 组卷打印        到期时生成 A4 PDF（题目页 + 独立答案页），打印出来
      ↓
⑤ 孩子重做        在纸上做
      ↓
⑥ 扫码回收        家长扫卷上二维码，逐题点对错
      ↓
⑦ 更新状态        答对进下一档、答错回第一档；连续通过全部档位则标记为已掌握
      ↓
    回到 ③
```

---

## 5. 功能需求

### FR-0 登录

| 项 | 内容 |
|---|---|
| 描述 | 两条入口：① 口令 → 我自己家的账号；② magic link `/join/<token>` → 该家庭的账号。除登录页与领取页外，任何页面与接口都要求已登录 |
| 规则 | 种下会话 cookie，有效期 30 天；首页右上角可退出 |
| 失败 | 口令错误提示「口令不对」；同一 IP 连续失败 8 次锁 10 分钟。magic link 无效时跳登录页，不区分「不存在」和「已撤销」 |
| 会话过期 | 页面自动跳回登录页并记住原位置，不要卡在「加载中」 |
| 数据隔离 | 每个家庭一个 account，四张主表的每个查询都按 `child_id` 收敛；越权一律 404 不是 403 |
| 验收 | 未登录访问任意页面被重定向到登录页；未登录调任意接口返回 401；A 家庭拿 B 家庭的 id 访问任意接口均得 404 |

### FR-0b 自助领取与专属入口

| 项 | 内容 |
|---|---|
| 描述 | 群里发一条 `/claim?c=<暗号>`，家长填个称呼就开一个自己的账号 |
| 规则 | `CLAIM_LIMIT` 名额上限（默认 0 = 关闭）、`CLAIM_CODE` 暗号、按 IP 限流（20 请求/小时、3 开户/小时） |
| 领取后 | 跳到 `/setup/<token>`：教他把应用**加到手机桌面**，并显示专属链接让他自己存好 |
| 关键约束 | 微信内置浏览器加不了桌面图标，必须先引导「在浏览器打开」；iOS 桌面应用的存储与 Safari 隔离，所以桌面图标指向 magic link，每次启动重新登录 |
| 验收 | 领取→装桌面→从桌面图标启动，能直接进首页，不需要再登录；名额满后再点领取链接得到明确提示 |

### FR-1 拍照上传

| 项 | 内容 |
|---|---|
| 描述 | 家长上传作业图片 |
| 规则 | 支持调用摄像头拍摄，也支持从相册选择。**实际实现为单张上传**——多页作业分多次拍，每次一页 |
| 限制 | 单张原图不超过 10MB。服务端会按 EXIF 摆正并压到最长边 2000px |
| 验收 | 在 iOS Safari 与 Android Chrome 上都能成功拍摄并上传；上传完成后自动进入圈题页 |

### FR-2 圈题标记

分两步完成，这是产品的核心操作。

**Step 1 — 圈出错题**

| 项 | 内容 |
|---|---|
| 图片操作 | 全屏显示原图，支持双指缩放、单指平移 |
| 自动检出框 | 当系统开启了自动切题时，页面加载后显示灰色半透明矩形框覆盖各题。点击某框 → 变为红色，标记为错题；再次点击 → 取消 |
| 手动添加框 | 单击图上任意未被框覆盖的位置 → 在该点为中心生成一个默认框（宽度 = 图片宽度的 80%，高度 = 宽度的 25%） |
| 调整框 | 选中框后可拖拽四角改变大小、拖拽框体移动位置；页面底部提供「宽度」「高度」两个滑块用于精细调整 |
| 删除框 | 选中框后点击框右上角的 × |
| 反馈 | 底部横向缩略条列出已圈的题，点击可定位到该框；顶部显示「已圈 N 道」 |
| 约束 | 至少圈出 1 道才能进入下一步 |
| 验收 | 在 375px 宽的屏幕上，圈出一道题的操作可在 5 秒内完成 |

**Step 1 完成方式**：主按钮是「保存 N 道」，圈完直接存。「逐题填答案」是次要入口。
**Step 2 整个可以跳过**——这是 v0.1 之后最重要的一次改动，原因见下。

**Step 2 — 逐题补充**（可选，一题一屏）

| 项 | 内容 |
|---|---|
| 顶部 | 进度指示「2 / 3」 |
| 预览 | 显示该题的裁剪结果，已添加的遮罩实时可见 |
| 遮罩工具 | 点击「加遮罩」→ 在预览图上拖出一个白色矩形，用于盖住孩子上次写的答案；可添加多个；点击已有遮罩可删除 |
| 正确答案 | 文本输入框，**选填**（初版是必填，已改） |
| 孩子答案 | 文本输入框，选填 |
| 完成 | 最后一题的按钮为「完成」，点击后保存全部题目 |
| 验收 | 完成后，这些题出现在错题本中，状态为「待复习」，下次复习日为明天 |

**正确答案为什么改成选填。** 这个字段的全部引用都是展示，没有一处参与逻辑判断：
生成 PDF 要的是题图和遮罩，扫码判对错家长对着原卷判，Leitner 状态机根本不碰它。
但它被设成必填后，圈 5 道错题要打字 5 次、超过 1 分钟——而这 1 分钟落在家长刚
检查完作业最不耐烦的时刻，是最可能导致两周内弃用的一处摩擦。

时机也是错的：圈题时老师若未订正，家长手里根本没有答案。真正需要答案、也真正
有答案的时刻是 FR-6 扫码回收——那时家长手里有原卷和孩子重做的，本来就在逐题看。
详见《痛点与针对性设计》§二。

### FR-3 错题记录

| 项 | 内容 |
|---|---|
| 描述 | 每道错题保存为一条独立记录 |
| 保存内容 | 题干裁剪图、遮罩位置、正确答案、孩子答案（若填）、来源（哪次拍摄）、创建时间 |
| 关键规则 | **每次作答单独记录一条历史**，不覆盖。同一道题在原始作业、第一次复习、第二次复习中的表现必须都能查到 |
| 验收 | 一道题被重做 3 次后，详情页能看到 4 条作答记录（1 次原始 + 3 次重做），每条含日期、来源、对错 |

### FR-4 复习调度

| 项 | 内容 |
|---|---|
| 算法 | Leitner 盒子法，5 档，间隔分别为 **1、3、7、14、30 天** |
| 新错题 | 进入第 1 档，下次复习日 = 明天 |
| 答对 | 档位 +1，下次复习日 = 今天 + 新档位对应的间隔天数 |
| 答错 | 档位重置为 1，下次复习日 = 明天 |
| 已掌握 | 在第 5 档答对 → 状态变为「已掌握」，退出常规复习，不再出现在到期列表中 |
| 逾期处理 | 超过复习日未做的题保持原档位，继续显示为到期，**不做任何惩罚性调整** |
| 验收 | 一道题连续答对 5 次后状态为「已掌握」；中途答错一次则退回第 1 档 |

### FR-5 组卷导出

| 项 | 内容 |
|---|---|
| 默认选题 | 所有下次复习日 ≤ 今天且状态为「复习中」的题 |
| 题数不足 | 到期题少于 4 道时提示「只有 N 道到期，是否加入 M 道即将到期的题？」，点击后按复习日由近到远补足至 4 道 |
| 手动调整 | 可从错题本挑选额外题目加入，也可取消已选题目 |
| 参数 | 每页题数（默认 5）、是否附答案页（默认是） |
| 输出格式 | **A4 PDF**，不提供其他格式 |
| 版式 | 见 §6 |
| 生成后 | 系统保存这张卷的题目和顺序，展示卷号（如 `R07`）和二维码，提供 PDF 下载 |
| 可重新下载 | 同一张卷可以反复下载，内容完全一致（孩子弄丢了可以重印） |
| 验收 | 生成的 PDF 打印在 A4 纸上，题目清晰可读，作答区留白充足，答案页独立 |

### FR-6 重做回收

| 项 | 内容 |
|---|---|
| 入口 | 扫描卷上二维码，或手动输入卷号 |
| 界面 | 按卷上顺序列出所有题，每行显示：题号短码、题干缩略图、正确答案、✓ 和 ✗ 两个按钮 |
| **补录答案** | 圈题时没填正确答案的题，这一行显示一个虚线输入框，判对错时顺手补。家长此时手里有原卷和孩子重做的，本来就在逐题看，边际成本接近零 |
| 快捷操作 | 顶部提供「全部标对」按钮，家长再单独把错的几道点成 ✗ |
| 提交 | 为每道题写入一条作答记录，并按 FR-4 规则更新复习档位 |
| 结果反馈 | 显示「对 5 道，错 2 道。2 道已回到第 1 档，明天再练。」 |
| 约束 | 同一张卷只能回收一次，回收后状态变为「已回收」 |
| 验收 | 回收后，错题本中对应题目的档位和下次复习日正确更新 |

### FR-7 错题管理

| 页面 | 功能 |
|---|---|
| 错题本列表 | 显示所有错题；每行：题干缩略图、正确答案、状态标签、下次复习日；支持按「全部 / 待复习 / 复习中 / 已掌握」筛选，按「最近添加 / 到期时间」排序 |
| 错题详情 | 显示题干图（默认应用遮罩，可切换查看原图）、正确答案、当前档位与下次复习日、完整作答历史时间线 |
| 编辑 | 可修改正确答案 |
| 手动干预 | 可手动标记为「已掌握」，或重置回第 1 档 |
| 删除 | 可删除错题（同时删除其作答历史） |

### FR-8 首页

| 卡片 | 内容 | 点击行为 |
|---|---|---|
| 今天要复习 | 到期题数 N、最早到期日 | N > 0 时跳转组卷页 |
| 待整理 | 已上传但未完成圈题的拍摄数 | 跳转继续圈题 |
| 待回收 | 已生成但未回收的复习卷 | 跳转回收页 |
| 统计条 | 错题总数 / 复习中 / 已掌握 | 跳转错题本对应筛选 |
| 主按钮 | 「拍作业」 | 跳转拍照页 |

---

## 6. PDF 版式规格

**纸张**：A4 纵向。

**页眉**（每页）
- 左侧：孩子姓名 · 卷号（如 `R07`）· 生成日期
- 右上角：二维码，尺寸 **64×64pt**，编码内容为该卷的回收链接
- 姓名下方：一行小字印出回收链接本身，作为**扫码失败时的退路**

50pt 时单模块只有 0.53mm，正好卡在家用打印机的可靠阈值上。扫码是整个闭环的
硬依赖——扫不出来家长就完全没法录结果，所以放大到 64pt（0.68mm/模块）。

**题目区**（每页默认 5 题）
- 每题左侧：两位短码（`01`、`02`…），用于回收时对应
- 短码右侧：题干裁剪图，等比缩放至内容区宽度，已应用遮罩（孩子上次的答案被白色矩形盖住）
- 题图下方：空白作答区，高度 = `clamp(题图高度 × 0.6, 12mm, 60mm)`

初版的「题图高度 × 0.6」方向就是错的：孩子的字高是固定的，跟题图多高无关。
实测口算行的题图只有 34pt 高，作答区就只剩 **7.2mm，写不下**；竖式题图 221pt 高，
作答区给到 46.9mm，纯浪费。越简单的题给的空间越小，正好反了。
- 一页放不下的题自动移到下一页，不允许跨页截断

**答案页**（可选，默认生成）
- 独立成页，位于全部题目之后
- 每行：短码 + 正确答案
- **只列填了答案的题**；一道都没填就整页不生成，否则印出来是一列光秃秃的题号

---

## 7. 非功能需求

| 类别 | 要求 |
|---|---|
| 平台 | 移动端网页。必须支持 iOS Safari、Android Chrome、微信内置浏览器 |
| 屏幕 | 最小适配宽度 375px（iPhone SE） |
| 性能 | 图片上传后 3 秒内进入圈题页；PDF 生成 10 秒内完成。**自动检出实测约 12 秒，未达成 5 秒要求**——改为后台异步：圈题页一打开就能手动圈，框到了再铺上去，用户感知不到延迟。已跑过的检出结果落库，重开同一张作业不再重跑 |
| 数据量 | 单个孩子一学期约 500 道错题、2000 张图片，需正常运行 |
| 可靠性 | 圈题过程中断（切后台、来电）后返回，已圈的框不丢失 |
| 隐私 | 图片存储在对象存储，不公开可访问，只能通过带签名的临时链接读取。全站需登录（FR-0），数据按家庭隔离 |
| 传输 | ⚠️ **当前是明文 HTTP**。HTTPS 已配好（Caddy + Let's Encrypt），但域名未备案被运营商阻断，只能走裸 IP。cookie 的 `Secure` 由 `BASE_URL` 推导，跟着降级。备案通过后改回 `BASE_URL` 即恢复 |

---

## 8. 明确不做（Out of Scope）

以下内容在 v0.1 中一律不实现，开发时遇到请勿自行扩展：

- ❌ 自动判对错（家长自己判断哪道错）
- ❌ 题干文字识别、知识点自动标注、错因分析
- ❌ 相似题生成、AI 讲题、语音辅导
- ❌ 数学以外的任何学科
- ❌ 用户账号体系、多用户、多孩子切换（数据结构预留，界面不做）
- ❌ 消息推送、短信提醒、微信通知
- ❌ 长图 PNG 导出、屏幕逐题练习模式
- ❌ 自动去手写（v0.1 靠家长手动拖遮罩）
- ❌ 小程序、App 打包、应用商店上架
- ❌ 付费、会员、任何商业化功能

---

## 9. 整体验收标准

开发完成的判定条件：

| # | 条件 | 状态 |
|---|---|---|
| 1 | 用一部手机走通完整链路：拍 1 页作业 → 圈 3 道错题 → 生成 PDF → 打印 → 扫码录结果 → 档位正确更新 | 🔴 **未走完**。拍作业/圈错题/详情已验证；**打印手感与扫码跳转未验证** |
| 2 | 连续答对 5 次变「已掌握」，中途答错退回第 1 档 | 🟢 `scripts/test-leitner.mjs` 9 条断言覆盖 |
| 3 | 同一张复习卷重复下载两次内容完全一致 | 🟢 `item_order` 持久化 |
| 4 | iPhone SE（375px）下圈题页所有操作可完成 | 🟢 已确认手感没问题 |
| 5 | 错题详情页能看到完整作答历史，每次重做独立记录 | 🟢 |
| 6 | 未登录访问任意页面/接口被挡住 | 🟢 已验证 |

**第 1 条是唯一的阻塞项**，需要真人拿纸和手机验证两件事：打印出来的 12mm 作答区
孩子够不够写、手机扫卷面二维码能不能跳转。

---

## 10. 后续版本（仅供开发理解扩展方向，不在本期范围）

| 版本 | 内容 |
|---|---|
| v0.2 | 账号体系、多孩子、到期提醒、家长周报 |
| v0.3 | 接入模型自动判对错，圈题页从「手动圈选」升级为「确认系统判定」 |
| v1.0+ | 题干 OCR、知识点标注、变式题生成、自动去手写 |

**架构上必须保证：v0.3 接入自动判对错时，只替换错题的产生方式，复习调度、组卷、回收等下游逻辑不需要修改。**
