# 切题检出测试方案

> 版本：v1 · 2026-09-02
> 相关：《系统架构.md》第五章（输入层三级自动化）
> 投入：一个晚上（约 3 小时）+ 几块钱调用费

---

## 〇、先修正上一轮的说法

上一轮我说「一个晚上的测试能省掉一周前端」。想细之后要修正：**省的不是一周。**

因为存在一条我当时没写出来的中间路径 **L0.5**：家长在错题上**点一下**（单击，不拖框），系统以点击点为中心裁剪，两个滑块粗调宽高。这个交互大约一天工作量，而不是一周——**完整的拖拽框选 UI 本来就可以绕开**，无论检出好不好。

所以这个测试仍然值得做，但理由变了：

1. **它决定的是「能省到什么程度」**，不是「要不要写一周前端」——最坏情况也只是一两天。
2. **检出是批改能力的下界。** 如果模型连一页作业切成几道题都搞不明白，那 L2 自动批改基本没戏。这是对整个项目预期的一次低成本校准。
3. **产出可复用。** 检出结果能直接缓存进后续的完整 benchmark，不是一次性消耗。

---

## 一、这个测试只回答一个问题

> **自动切题够不够准，能让家长只用「点」而不用「框」？**

它**不**回答：判对错准不准、OCR 认字准不准、置信度可不可信。那些是《模型准确率基准测试方案》的事。

**唯一的输出是一个 UI 决策。**

---

## 二、失败模式分析：为什么主指标是「错题召回率」

不同的检出错误，在有人在环的交互里代价完全不同：

| 失败模式 | 现象 | 家长要做什么 | 代价 |
|---|---|---|---|
| **虚检**（多切出不存在的题、把插图当题） | 多几个候选框 | 不点它 | **几乎为零** |
| **切碎**（一题被切成两个框） | 一道题出现两个候选 | 点两下，或需要合并 | 低 |
| **粘连**（两题挤在一个框里） | 框里带了无关的题 | 需要拆分或微调 | 中 |
| **框歪 / 框小**（截断了题干或答案） | 裁剪图内容不完整 | 必须调边界 → 要拖拽 | **高** |
| **漏检**（错题根本没被框出来） | 家长找不到要点的题 | 必须能手动补 → 要拖拽 | **高** |

两个结论直接决定 prompt 怎么写和指标怎么定：

**结论 1：虚检几乎免费，漏检致命。** 所以要把模型调到**高召回、容忍低精确率**——宁可多切，不可少切。prompt 里要明确说「宁可把不确定的区域也框出来」。这跟大多数人默认追求的 F1 平衡是相反的。

**结论 2：只有错题需要被检出。** 做对的题漏了无所谓——家长根本不会去点它。所以主指标是**错题召回率**，不是全题召回率。这是个很大的门槛降低，别用全题召回率把自己吓退。

---

## 三、候选方案：VLM 不一定是最优解

大多数人的默认动作是「让多模态模型输出 bounding box」。**这很可能是四个方案里最差的一个**——坐标定位（grounding）是 VLM 的传统短板，输出的框经常整体偏移或尺度错乱。

四个方案都要测，成本都很低：

| 方案 | 做法 | 优势 | 风险 |
|---|---|---|---|
| **B0 VLM 直接出框** | 让视觉模型输出每道题的 bbox | 最省事，能处理任意版式 | 坐标可能整体不准；不同模型的坐标系约定不一（归一化 vs 像素） |
| **B1 题号锚点** ⭐ | OCR 定位所有题号（`1.` `2.` `(3)` `①`），相邻题号之间即一道题 | **零模型、可解释、成本为零**；小学作业几乎必有题号 | 无题号的页（纯口算列）失效；双栏版式要处理列序 |
| **B2 投影切分** | 水平投影找行间空白带，按空白切行再聚合 | 纯 CV，对口算页和竖式页可能极准 | 应用题多行连续，切不开 |
| **B3 版面分析模型** | PaddleOCR 版面分析等现成开源模型本地跑 | 免费、本地、中文文档上成熟 | 是为印刷版面训练的，作业本有手写，可能不适配 |

**我押 B1 题号锚点。** 它被低估了：小学作业的题号是极强的结构信号，OCR 找几个数字比理解整页版面容易得多，而且**结果完全可解释**——错了你知道为什么错，而 VLM 出的坏框你只能干瞪眼。

实际最终方案很可能是 **B1 为主 + B0 兜底**（无题号的页才调模型）。

测试顺序：先 B0 和 B1（一个代表默认做法，一个代表押注），够用就停；不够再加 B2/B3。

---

## 四、数据：12–15 页，主变量是版式

检出的方差主要来自**版式**，不是字迹好坏。别按「工整/潦草」分层，按版式分层：

| 版式类型 | 页数 | 为什么要 |
|---|---:|---|
| 口算页（密集小题、网格排列） | 3 | 题最多，切错代价最大；也是 B2 投影法最可能赢的地方 |
| 竖式 / 脱式计算页 | 2 | 题目是二维块状，不是单行 |
| 应用题页（大段文字，3–6 题） | 3 | 题号锚点法的主场 |
| 试卷（混合题型，可能双栏） | 3 | 双栏是所有方案的公共难点 |
| 带插图 / 几何图形的页 | 2 | 测虚检——插图会不会被当成题 |
| 练习册（彩色底纹、花哨排版） | 2 | 最脏的真实样本 |

从旧作业本里挑，**一个晚上够拍完**。

---

## 五、评估方法：目视评审，不做 IoU

这是本方案最重要的省事设计。

上一版完整 benchmark 里我说「最难的是把预测题和真值题对上」。**这次直接绕过这个问题：不标注框坐标，不算 IoU，不写匹配算法。**

流程：

1. **拍 12–15 页**，编号存好
2. **人工记真值**：每页记下「总题数」和「哪几道是错题」。旧作业本上红笔已经批过了，直接抄，两分钟一页
3. **跑各方案**，输出每道题的框坐标
4. **脚本把框画回原图**，生成带彩色标注的可视化图（每个框标上序号）
5. **打开可视化图，人眼逐页打分**，填评分表

**没有精确标注、没有匹配算法、没有 IoU 阈值调参。** 这就是它能在一个晚上做完的原因。

> 唯一需要写的代码是「调用 + 画框 + 拼图」，大约 60 行。

### 评分表（每页一行）

| 字段 | 怎么填 |
|---|---|
| `页ID` | p01… |
| `版式` | 口算 / 竖式 / 应用题 / 试卷 / 插图 / 练习册 |
| `真值题数` | 数出来的 |
| `真值错题数` | 红笔打叉的 |
| `检出框数` | 脚本输出 |
| **`漏检错题数`** | **主指标**：有几道错题没有任何框覆盖 |
| `漏检对题数` | 参考，代价低 |
| `虚检框数` | 框住了非题目区域（插图、页眉、空白） |
| `切碎数` | 一道题被切成多个框 |
| `粘连数` | 多道题挤在一个框里 |
| **`边界质量`** | **主指标**：好（题干+答案区完整）/ 一般（略有出入但能用）/ 差（截断了内容） |

---

## 六、判据：三档 UI 决策

汇总所有页，算两个数：

- **错题召回率** = 1 − (总漏检错题数 / 总真值错题数)
- **边界可用率** = 评为「好」或「一般」的框占比

| 结果 | 输入层做法 | 前端工作量 |
|---|---|---|
| 🟢 错题召回 ≥ 90% **且** 边界可用 ≥ 85% | **L1 纯点选**：显示所有检出框，家长点错的那几道 | ~1 天 |
| 🟡 错题召回 70–90%，或边界一般 | **L0.5 混合**：家长点一下 → 就近吸附到检出框；附近没框就用默认矩形 + 滑块微调 | ~2 天 |
| 🔴 错题召回 < 70% | **L0.5 纯点击**：完全不用检出，点击点 + 默认矩形 + 两个滑块 | ~1 天 |

注意两件事：

1. **完整的拖拽框选 UI 在三档里一次都没出现。** 这就是第〇章那个修正的意思。
2. **最坏的结果反而不是最贵的。** 🔴 比 🟡 还省，因为不用写吸附逻辑。所以别怕测出红灯——测出红灯你直接省掉一整块复杂度。

真正糟糕的情况只有一种：**结果落在 🟡 的下沿，检出半好不坏。** 那你既要做吸附又要做兜底，两套逻辑都得维护。真遇到这种，**建议主动降级到 🔴 做法**，用确定性差但简单的方案换掉不稳定的智能。

---

## 七、执行清单（约 3 小时）

| 步骤 | 时间 |
|---|---|
| 从旧作业本挑并拍 12–15 页，按第四章分层核对一遍 | 30 min |
| 记录真值（每页题数 + 错题号） | 20 min |
| 写脚本：调用 + 画框 + 生成可视化图 | 60 min |
| 跑 B0 和 B1 | 20 min |
| 目视评审打分，填表 | 40 min |
| 汇总两个数，对照第六章下结论 | 10 min |

**成本**：15 页 × 2–4 方案 ≈ 60 次调用，几块钱。B1/B2/B3 本地跑，免费。

### 目录

```
bench-detect/
  images/         # 原图 p01.jpg …
  truth.csv       # 页ID, 版式, 真值题数, 真值错题数, 错题题号
  runs/{方案}/     # 框坐标 json
  viz/{方案}/      # 画好框的可视化图 ← 你要看的就是这些
  score.csv        # 评分表，手填
  detect.py        # 调用 + 画框，约 60 行
```

---

## 八、不做的事

- ❌ 不测判对错、不测 OCR 转写、不测置信度
- ❌ 不标框坐标、不算 IoU、不写匹配算法
- ❌ 不调 prompt 超过两轮——这是测「现成能力够不够」，不是做 prompt 工程
- ❌ 不为了好看去挑版式规整的作业
- ❌ 不把脚本写成可复用框架，它是一次性的

**唯一交付物：两个数（错题召回率、边界可用率）+ 一个 UI 档位决定。**
