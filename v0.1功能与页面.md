# v0.1 功能与页面设计

> 版本：v1 · 2026-09-02
> 范围：单孩子、无账号、本地或单机部署，自己先用两周。

---

## 〇、术语

| 术语 | 含义 |
|---|---|
| **IoU**（交并比，Intersection over Union） | 两个矩形框重叠部分的面积 ÷ 两个框合起来覆盖的总面积。取值 0–1，1 表示完全重合。目标检测里用它判断「预测框算不算命中真值框」，一般以 0.5 为阈值。上一份检出测试方案里说不用 IoU，是因为要算它就得先精确标注每道题的真实框坐标，很费时间；改成人眼看画好框的图直接打分，省掉标注这一步。 |
| **Leitner 盒子** | 一种复习间隔法。题目分成若干「盒子」，答对进下一个盒子（间隔变长），答错退回第一个盒子（间隔重置）。本项目用 5 个盒子，间隔 1/3/7/14/30 天。 |
| **遮罩** | 盖在题干截图上的白色矩形，用来挡住孩子上次写的答案。存的是坐标，不修改原图，组卷渲染时才应用。 |
| **presigned URL**（预签名链接） | 对象存储生成的一个带签名、有有效期的临时链接。浏览器拿它可以直接上传或下载文件，不用把存储密钥给前端，图片也不经过自己的服务器中转。 |
| **serverless 超时** | Vercel 这类平台上每个后端函数有最长执行时间限制（免费档 10 秒，Pro 60 秒），超时直接中断。调用大模型经常超过 10 秒，是这类部署的主要坑。 |
| **归一化坐标** | 把框的坐标存成相对图片宽高的比例（0–1），而不是像素值。这样换模型、换图片尺寸都不用重算。 |

---

## 一、页面清单

| # | 页面 | 路由 | 作用 |
|---|---|---|---|
| P1 | 首页 | `/` | 今天要做什么 |
| P2 | 拍照上传 | `/capture` | 传图 |
| P3 | 圈题 | `/capture/[id]/mark` | **核心页**，把错题变成数据 |
| P4 | 错题本 | `/mistakes` | 列表 |
| P5 | 错题详情 | `/mistakes/[id]` | 单题历史 |
| P6 | 组卷 | `/review/new` | 生成 PDF |
| P7 | 回收 | `/review/[code]/collect` | 重做结果录入 |

底部导航三项：首页 / 错题本 / 我的。

---

## 二、各页规格

### P1 首页

| 区块 | 内容 | 操作 |
|---|---|---|
| 卡片「今天要复习」 | 到期错题数 N、最早到期日 | N>0 → 按钮「生成复习卷」跳 P6 |
| 卡片「待整理」 | 已上传但未圈题的拍摄数 M | M>0 → 点击跳 P3 继续 |
| 卡片「待回收」 | 已生成未回收的复习卷 | 点击跳 P7 |
| 主按钮 | 「拍作业」 | 跳 P2 |
| 统计条 | 错题总数 / 复习中 / 已掌握 | 点击跳 P4 对应筛选 |

### P2 拍照上传

- 两个入口：调用摄像头、从相册多选
- 支持连续多张（一份作业多页）
- 上传中显示进度和缩略图，可删除重拍
- 全部上传完成 → 自动跳 P3

### P3 圈题（核心页，分两步）

**Step 1 — 圈出错题**

| 元素 | 行为 |
|---|---|
| 图片区 | 全屏显示原图，双指缩放、单指平移 |
| 自动检出框 | 若检出可用：灰色半透明矩形铺在图上。点击 → 变红，标记为错题；再点 → 取消 |
| 手动补框 | 单击图上任意空白处 → 生成默认框（宽 = 图宽 80%，高 = 宽 × 25%），中心在点击点 |
| 框调整 | 选中框后拖四角调大小、拖框体移动；底部备两个滑块（宽 / 高）用于精调 |
| 删除框 | 选中框 → 长按或点框上的 × |
| 底部缩略条 | 横向列出已圈的题，点击定位 |
| 计数 | 「已圈 3 道」 |
| 按钮 | 「下一步」（至少 1 道才可点） |

**Step 2 — 逐题补充**（一题一屏，左右滑动切换）

| 元素 | 行为 |
|---|---|
| 顶部进度 | 「2 / 3」 |
| 裁剪图预览 | 显示该题裁剪结果，已应用的遮罩实时可见 |
| 遮罩工具 | 点「加遮罩」→ 在预览图上拖一个白色矩形；可加多个；点已有遮罩可删 |
| 正确答案 | 文本输入框，**必填** |
| 孩子答案 | 文本输入框，选填（v0.1 可全部跳过） |
| 按钮 | 「下一题」/ 最后一题显示「完成」 |

**完成时写入**：每道题 → 1 条 `problem` + 1 条 `attempt`(verdict=wrong, source=manual) + 1 条 `mistake_card`(box=1, next_due=明天)

### P4 错题本

- 列表行：裁剪图缩略（80×80）| 正确答案 | 状态标签 | 下次复习日
- 顶部筛选 tab：全部 / 待复习 / 复习中 / 已掌握
- 排序：最近添加 / 到期时间
- 空状态：「还没有错题，去拍一份作业」

### P5 错题详情

| 区块 | 内容 |
|---|---|
| 图片 | 裁剪图（默认显示应用遮罩后的版本，可切换看原始） |
| 正确答案 | 文本，可编辑 |
| 掌握状态 | 「Box 3 / 5，下次复习 2026-09-15」 |
| 作答历史 | 时间线，每条：日期 · 来源（原始作业 / 复习卷 R07）· ✓ 或 ✗ |
| 操作 | 手动标为已掌握 / 重置到 Box1 / 删除 |

### P6 组卷

| 区块 | 内容 |
|---|---|
| 默认选中 | 所有到期题（`next_due_date <= 今天`） |
| 题数不足提示 | 少于 4 道时：「只有 3 道到期，是否加入 5 道即将到期的？」→ 一键补齐 |
| 手动增减 | 可从错题本挑题加入、取消已选 |
| 设置 | 每页题数（默认 5）、是否附答案页（默认是） |
| 预览 | 显示将生成的题目缩略列表 |
| 按钮 | 「生成 PDF」 |

生成后：写入 `review_sheet`（含短码、题目顺序），返回 PDF 下载链接，跳结果页显示短码和二维码。

**PDF 版式（A4）**

- 页眉：孩子名 · 卷号 `R07` · 生成日期 · 右上角二维码（编码 sheet id）
- 每题：左侧短码 `07` + 题干裁剪图（已应用遮罩）+ 下方留白作答区（高度 = 题图高度 × 0.6）
- 每页 5 题，超出自动分页
- 末尾单独答案页：短码 + 正确答案，一行一题

### P7 回收

| 步骤 | 内容 |
|---|---|
| 入口 | 扫二维码，或手动输入短码 `R07` |
| 题目列表 | 按卷上顺序列出，每行：短码 + 裁剪图缩略 + 两个大按钮 ✓ / ✗ |
| 快捷操作 | 顶部「全部标对」按钮，再单独点错的那几道 |
| 提交 | 按钮「提交」 |
| 结果页 | 「对 5 道，错 2 道。2 道已回到第 1 盒，明天再练。」 |

**提交时写入**：每题 → 1 条 `attempt`(capture_id 可空, source=manual) + 更新 `mistake_card`（答对 box+1、重算 next_due；答错 box=1、next_due=明天；box 达 5 且答对 → status=mastered）

---

## 三、数据模型

```sql
child (
  id, name, grade, created_at
)

capture (                       -- 一次拍摄事件
  id, child_id,
  image_key,                    -- 对象存储 key
  source_type,                  -- 'homework' | 'exam' | 'review_redo'
  review_sheet_id,              -- 仅 review_redo 时有值
  created_at
)

problem (                       -- 一道题的身份
  id, child_id,
  source_capture_id,
  crop_box,                     -- jsonb，归一化坐标 {x,y,w,h} 相对原图
  crop_image_key,               -- 裁好的图
  mask_boxes,                   -- jsonb 数组，归一化坐标，相对裁剪图
  correct_answer  text,
  stem_text       text null,    -- v0.1 留空，OCR 接入后填
  created_at
)

attempt (                       -- 一次作答，同一题多条
  id, problem_id,
  capture_id      null,         -- 回收时可为空
  child_answer    text null,
  verdict,                      -- 'right' | 'wrong' | 'blank' | 'unclear'
  source,                       -- 'manual' | 'model'
  confidence      real null,
  created_at
)

mistake_card (                  -- 复习调度单位，与 problem 一对一
  id, problem_id unique,
  box_level       int,          -- 1..5
  next_due_date   date,
  consecutive_correct int,
  status,                       -- 'learning' | 'mastered'
  updated_at
)

review_sheet (
  id, child_id,
  short_code      text unique,  -- 'R07'
  item_order      jsonb,        -- [{seq:1, problem_id, code:'01'}, ...] 持久化，可重新生成同一份
  status,                       -- 'generated' | 'collected'
  pdf_key,
  created_at
)
```

盒子间隔：`{1:1, 2:3, 3:7, 4:14, 5:30}` 天。

---

## 四、模型部署与调用

### 4.1 v0.1 只有一处用模型

| 用途 | 版本 | 状态 |
|---|---|---|
| **切题检出**（P3 Step1 的灰色框） | v0.1 | 可选，没有也能跑（家长手动点击生成框） |
| 判对错 | v0.3 | 待 benchmark |
| 题干 OCR / 知识点标注 | v2 | — |

**没接模型系统也完整可用。** 先按无模型跑通，检出作为增强后加。

### 4.2 两条检出路线

**路线 A — OCR + 题号锚点（推荐先做）**

1. 调云 OCR「通用文字识别（含位置）」，返回每个文本块 `{text, box}`
2. 自己写逻辑：正则匹配题号（`^\d+[.、]`、`^[(（]\d+[)）]`、`①②③`）→ 按 y 坐标排序 → 相邻两个题号之间的区域即一道题

- 成本：千次调用几块钱，各家都有免费额度
- 延迟：1–2 秒
- 切分逻辑全在自己代码里，出错可调试

**路线 B — VLM 直接出框**

调多模态模型，prompt 要求返回每道题的框坐标 JSON。

- 延迟：5–15 秒
- 注意：各家坐标基准不同（0–1 小数 / 0–1000 整数 / 原图像素），接入时先用一张图验证，统一转成归一化坐标再存

### 4.3 供应商与接入点

| 用途 | 供应商 | Base URL | 注意 |
|---|---|---|---|
| OCR | 阿里云 / 腾讯云 / 百度智能云 | 各家 SDK | 选「通用文字识别高精度版」，要**带位置坐标**的那个接口 |
| VLM | 阿里云百炼 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | OpenAI 兼容格式，直接用 openai sdk 改 baseURL |
| VLM | 火山方舟 | `https://ark.cn-beijing.volces.com/api/v3` | **要先在控制台创建「推理接入点」，请求里填 endpoint id 而不是模型名** |
| VLM | 智谱 | `https://open.bigmodel.cn/api/paas/v4` | GLM-4V 系列 |
| VLM（仅自用对照） | Anthropic | 官方 SDK | `claude-opus-5` / `claude-sonnet-5`。数据出境，**只在自己家阶段用，开放给家长群前必须换成国内模型** |

具体模型 ID 去各家控制台看当前在售版本，不要照抄网上旧文章里的 ID。

### 4.4 调用链路

```
浏览器
  │ ① GET  /api/upload-url        取 presigned URL
  │ ② PUT  <presigned URL>        图片直传对象存储，不经过自己服务器
  │ ③ POST /api/detect {image_key}
  ▼
Next.js API Route（服务端）
  │ ④ 由 image_key 生成带签名的临时可读 URL
  │ ⑤ 调 OCR / VLM（API key 只存在服务端环境变量）
  │ ⑥ 解析结果 → 转成归一化坐标 → 返回框数组
  ▼
浏览器渲染灰色框
```

### 4.5 四个必须处理的点

1. **API key 只能在服务端。** 所有模型调用走 `/api/*` route，前端永远不碰密钥。
2. **serverless 超时。** Vercel 免费档 10 秒、Pro 60 秒，VLM 调用容易超。
   - v0.1 自己用：本地 `next dev`，无超时限制
   - 开放给家长群：换 Railway / Fly.io / 便宜 VPS（不限时），或把检出改成异步——上传后后台跑，前端轮询 `/api/detect/status`
3. **图片怎么给模型。** 用对象存储的临时签名 URL（推荐），不要走 base64 内联——Vercel 请求体上限 4.5MB，一张作业原图就可能超。
4. **坐标统一归一化后存储。** 入库前一律转成相对比例 0–1，换模型、换图片尺寸都不受影响。

### 4.6 环境变量

```
DATABASE_URL=

S3_ENDPOINT=
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=

DETECT_MODE=none|ocr|vlm      # none 时 P3 只有手动点击
OCR_PROVIDER=
OCR_KEY=
OCR_SECRET=

VLM_BASE_URL=
VLM_API_KEY=
VLM_MODEL=
```

---

## 五、技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 全栈框架 | Next.js（App Router） | 前后端一个项目 |
| 数据库 | Postgres（Neon 或 Supabase 免费档） | 10–50 个家庭够用 |
| 对象存储 | Cloudflare R2 或阿里云 OSS | 图片不进数据库 |
| 图片处理 | `sharp` | 服务端裁剪、压缩、应用遮罩 |
| PDF 生成 | `pdf-lib` | 纯 JS，能嵌入 JPG/PNG。不用 puppeteer——在 serverless 上太重 |
| 二维码 | `qrcode` | 生成 PNG 嵌进 PDF |
| 前端图片交互 | 原生 canvas 或 `react-konva` | P3 的框选是唯一有难度的前端工作 |

**部署路径**：v0.1 本地 `next dev` 自己用 → 给家长群时部署到 Railway / Fly.io（不限函数执行时长）→ 链接直接发群里，浏览器打开。不做小程序、不发应用商店、不备案。

---

## 六、开发顺序

| 步骤 | 内容 | 预估 |
|---|---|---|
| 1 | 建库、建表、对象存储打通 | 半天 |
| 2 | P2 上传 + P3 Step1 框选交互（**先做这个，最难**） | 2–3 天 |
| 3 | P3 Step2 遮罩 + 答案录入、裁剪落库 | 1 天 |
| 4 | P4 / P5 列表与详情 | 半天 |
| 5 | Leitner 状态机 + P1 首页 | 半天 |
| 6 | P6 组卷 + PDF 渲染 + 二维码 | 1–2 天 |
| 7 | P7 回收 + 状态更新 | 半天 |
| 8 | 接检出（可选） | 半天 |

先做第 2 步，不要先建数据库。框选交互跑不顺，后面全白搭。
