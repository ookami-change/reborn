# 痛点与针对性设计

> v1 · 2026-09-03
> 对象：《市场调研》§5.2 反复出现的负面评价、§二 用户访谈痛点、《商业化瓶颈》§二。
> 只写**能落到代码上的设计**，商业化/合规类痛点在《商业化瓶颈.md》。

---

## 〇、术语

| 术语 | 含义 |
|---|---|
| **主闭环** | 拍作业 → 圈错题 → 生成复习卷 → 打印 → 孩子重做 → 扫码录对错 → 更新档位。少任何一环，产品不成立。 |
| **摩擦** | 家长为完成一次主闭环付出的操作步数与时间。摩擦决定留存，功能决定上限。 |
| **假掌握** | 孩子在复习卷上答对，但只是记住了这道题的答案，换个数字仍然不会。Leitner 会把它判为已掌握。 |
| **FPR**（假阳率） | 系统说「这道错了」但其实是对的，占全部正确题的比例。家长会看见并且必须去改，是**吵闹的失败**。 |
| **漏检** | 系统没发现的错题。家长看不见，**安静的失败**，危害更大但不产生投诉。 |
| **origin** | 一个框是模型给的（detected）还是家长手画的（manual）。这两者的差异是训练检出模型唯一的免费标注来源。 |

---

## 一、痛点清单与 v0.1 现状

| # | 痛点（出处） | v0.1 现状 | 本文档 |
|---|---|---|---|
| P1 | 拍照、裁切、选科目、写备注步骤过多（§5.2）<br>**手动填正确答案，家长会直接放弃**（瓶颈§二） | 🔴 **更严重**：填答案是必填，卡住保存 | §二 |
| P2 | ——（未被文档列出，实现时产生的） | 🔴 训练信号每次都在丢 | §三 |
| P3 | 正确答案被判错 / 错题没被识别，不敢信任（§5.2） | ⚪ 未接批改模型，尚未发生 | §四 |
| P4 | 收藏了错题却没推动孩子重做（§二） | 🟢 已设计（Leitner + 二维码回收） | §五 |
| P5 | 假掌握 | 🟡 已识别未处理 | §五 |
| P6 | 题型、字号、答案排版不统一（§5.2） | 🟢 已解决（固定 A4 模板） | §六 |
| P7 | 多 App 之间切换（§二） | 🟢 不适用（单应用闭环） | — |
| P8 | AI 讲解用了孩子没学过的方法（§5.2） | ⚪ v2 才有 | §六 |
| P9 | 高峰期响应慢 / 排队（§5.2） | 🟡 检出 12s + RPM 3 | §六 |
| P10 | 会员墙、自动续费、皮肤金币诱导敷衍（§5.2） | 🟢 不做（见§七） | §七 |

优先级只有一条判据：**会不会导致家长在两周内停用**。P1 会，P2 是不可逆的机会成本，其余都可以等。

---

## 二、P1 —— 必填的正确答案（最高优先级）

### 2.1 现状有多重

三个地方联手把它变成硬门槛：

| 位置 | 代码 | 效果 |
|---|---|---|
| `lib/db/schema.ts:56` | `correctAnswer: text("correct_answer").notNull()` | 数据库层面不允许为空 |
| `app/api/captures/[id]/problems/route.ts:27` | `if (items.some((i) => !i.correctAnswer?.trim())) return 400` | 接口硬拒 |
| `app/mark/[id]/page.tsx:143` | `disabled={!detail.correctAnswer.trim()}` | 按钮点不动 |

圈 5 道错题 = 5 次进入 Step 2 逐题页 + 5 次打字。实测每题 10–15 秒，**一次超过 1 分钟**，而这 1 分钟发生在家长刚检查完作业、最不耐烦的时刻。

### 2.2 关键事实：主闭环根本不需要它

把 `correctAnswer` 的全部 20 处引用查了一遍，**没有一处参与逻辑判断，全部是展示**：

| 用途 | 位置 | 缺了会怎样 |
|---|---|---|
| 复习卷答案页印一行 | `lib/pdf.ts:143` | 那一行空着 |
| 错题列表显示「答案 X」 | `app/mistakes/page.tsx:53` | 显示空 |
| 组卷预览显示 | `app/review/new/page.tsx:135` | 显示空 |
| 回收页每行显示 | `app/review/[code]/collect/page.tsx:102` | 显示空 |
| 详情页可编辑 | `app/mistakes/[id]/page.tsx:110` | —— |

生成 PDF **不需要**（要的是题图 + 遮罩）；扫码回收判对错**不需要**（家长手里有原卷和孩子重做的，本来就是对着纸判的）；Leitner 状态机是纯函数，**碰都不碰它**。

**结论：一个纯展示字段被设成了主流程的必经门槛。这是设计错误，不是取舍。**

### 2.3 更根本的问题是时机

「圈题时」是录答案最糟的时机：

| 时刻 | 家长手里有什么 | 有没有答案 | 有没有动力录 |
|---|---|---|---|
| 圈题时 | 刚批改完的作业本 | 老师若未订正，**根本没有** | 无，孩子今天不做 |
| 打印时 | 复习卷 | —— | 无 |
| **回收时** | 原卷 + 孩子重做的 | 有（要判对错必须有） | **有，正在逐题看** |

答案该在回收时顺手录，不该在圈题时强制录。

### 2.4 设计

**主路径改为：圈完即存，Step 2 整个可跳过。**

```
拍照 → 自动切题 → 点选哪几道错了 → 【保存】     ← 约 10 秒，5 道题
                                    ↓ 可选
                            逐题填答案/画遮罩     ← 想填才进
```

改动清单：

| # | 文件 | 改动 |
|---|---|---|
| 1 | `lib/db/schema.ts` | `correct_answer` 去掉 `notNull()`，改 `.default("")` |
| 2 | `drizzle/0001_*.sql` | `ALTER TABLE problem ALTER COLUMN correct_answer DROP NOT NULL` |
| 3 | `api/captures/[id]/problems/route.ts:27` | 删掉那条 400 校验 |
| 4 | `app/mark/[id]/page.tsx` | Step 1 头部「下一步」旁加**「直接保存」**；Step 2 底部按钮不再 `disabled` |
| 5 | `lib/pdf.ts:136` | 答案页只列非空的；一道都没有则**不生成答案页** |
| 6 | `app/review/[code]/collect/page.tsx` | 每行答案位置：有则显示，无则给一个**行内输入框**，判对错时顺手补 |
| 7 | `api/review/sheets/[code]/collect/route.ts` | 接受可选的 `correctAnswer`，一并写回 `problem` |

第 6 条是重点——**把录入挪到了家长本来就在逐题操作的那一屏，边际成本接近零**。

### 2.5 三条补答案的辅助路径（按性价比排序）

| 路径 | 怎么做 | 单题耗时 | 适用 | 何时做 |
|---|---|---|---|---|
| **回收时顺手填** | 见上，行内输入框 | ~3s | 全部题型 | 随 §2.4 一起 |
| **框住纸上的答案** | 老师红笔订正过的，圈错题时多拖一个小框 → 存成图，答案页贴图 | ~5s，**零打字** | 老师已订正 | 复用 `MaskEditor`，约半天 |
| **模型算** | 检出模型顺便读题干并计算 | 0s | **仅口算题** | 见《模型选型与训练路线》§三 |

第三条有反噬风险：模型算错 → 家长照着错答案判孩子做对的题为错 → 比没有答案更糟。所以**必须默认不填、只作为灰色建议，家长点一下才采纳**，并在 `problem` 上记来源。

### 2.6 验收

| 指标 | 现状 | 目标 |
|---|---|---|
| 圈 5 道错题到保存完成 | ~75s | **< 15s** |
| 必须打字的字符数 | 每题 1–8 | **0** |
| 一周后仍有答案的错题占比 | —— | > 60%（靠回收时补） |

---

## 三、P2 —— 训练信号每次都在丢（现在改，成本近零）

`lib/types.ts` 里 `MarkedBox` 有 `origin: "detected" | "manual"`，前端全程带着它。但 `problem` 表**没有这一列**，`problems/route.ts` 落库时把它丢了。

丢掉的是什么：

| 家长的动作 | 等价于什么标注 | 现在存了吗 |
|---|---|---|
| 直接采纳模型给的框 | 一条「检出正确」正样本 | ❌ |
| 手画一个模型没给的框 | 一条**漏检**样本（最值钱） | ❌ |
| 拖动/缩放模型给的框 | 一条框回归的修正样本 | ❌ |
| 无视模型给的框 | 一条虚检样本 | ❌ |

这跟《商业化瓶颈》§五是同一个论点：**现在加两列，以后是数据迁移**。而且这批数据是自训检出模型唯一的免费来源——真实作业、真实拍摄条件、真实标注，一张都不用花钱买。

设计：

| 新增 | 类型 | 含义 |
|---|---|---|
| `problem.box_origin` | `text` | `detected` / `manual` |
| `problem.box_adjusted` | `boolean` | 模型给的框是否被家长改过 |
| `capture.detected_boxes` | `jsonb` | 模型这次输出的**全部**框（含家长没选的），带模型名与耗时 |

第三条最关键：只存被采纳的框，永远得不到虚检和漏检的负样本。存全量才能算召回率。

配套：一个 `scripts/export-dataset.mjs`，把 `capture.image_key + detected_boxes + problem.crop_box` 导成 YOLO/COCO 格式。攒够了直接能训，不用回头考古。

---

## 四、P3 —— 误判不敢信任（接批改模型前必须先建）

《市场调研》§5.2 排第一的负面评价。v0.1 还没接批改模型，所以现在是**唯一能在造成伤害前把机制建好**的时间点。

三条设计：

### 4.1 宁可漏报，不可错报

FP 和 FN 的代价不对称：错报家长立刻看见并且要动手改，漏报家长根本不知道。所以阈值不是取「准确率最高」那一点，而是**在 FPR < 0.9% 的约束下取召回最高**（推导见《模型准确率基准测试方案》）。低于置信阈值的题**不标对错，标「不确定」交给家长看一眼**。

`attempt` 表的 `verdict` 已经预留了 `unclear`，`confidence` 也有——这部分设计是对的，不用改。

### 4.2 可追溯：家长问「凭什么判我孩子错」必须能答

错题详情页加一块折叠区，展示：原图裁剪 + 模型原始输出 + 置信度 + 模型名与版本。`attempt.source` 已有，需要补 `attempt.model_version` 和 `attempt.raw_output`。

### 4.3 申诉即标注

家长点「判错了」→ 不是简单撤销，而是写入一张 `correction` 表：

| 字段 | 用途 |
|---|---|
| `attempt_id` | 指向被推翻的那次判定 |
| `model_verdict` / `human_verdict` | 模型说什么、人说什么 |
| `created_at` | —— |

这张表同时是三样东西：投诉记录、**评测集**、微调训练集。误判从纯负债变成资产。目标：接入批改模型后，评测集里 **≥ 30% 的样本来自真实申诉**，而不是我自己造的。

---

## 五、P4/P5 —— 推动重做与假掌握

### 5.1 已解决的部分

| 机制 | 实现 |
|---|---|
| 首页「今晚要做什么」 | `api/home` 聚合到期数量 |
| Leitner 5 档，1/3/7/14/30 天 | `lib/leitner.ts` 纯函数 |
| 复习卷印二维码，扫码即回收 | `lib/pdf.ts` + `review/[code]/collect` |
| 逾期不惩罚，保持原档继续到期 | `isDue` 用 `<=` 比较 |

二维码是这里的关键设计：它把「录结果」从一件要主动打开 App 的事，变成了卷子上一个物理入口。

### 5.2 假掌握：识别了，v0.1 不做

孩子答对可能是记住了这道题。5 档连续答对 = 掌握，中间没有任何变式。

三种解法与代价：

| 解法 | 需要 | v0.1 |
|---|---|---|
| 出变式题（换数字） | 题目结构化解析（OCR + 语义） | ❌ 远超范围 |
| 同一道题**间隔拉长**后再考 | 已经是 Leitner 在做的（30 天） | ✅ 现有机制部分覆盖 |
| 第 5 档答对时**不直接毕业**，随机延后一次 | 改 `applyAttempt` 十几行 | 🟡 **可做，但先观察** |

**决定：v0.1 不改。** 先跑两个月，看真实的「已掌握后又答错」比例。这个数字现在没有，凭空调参数是瞎猜。数据来源：`attempt` 表本来就不覆盖旧记录，直接查得到。

---

## 六、P6/P8/P9 —— 已解决、未到时候、可接受

| # | 结论 |
|---|---|
| **P6 排版** | 已解决。固定 A4 模板，题号列宽、图宽、作答区比例全是常量；**题目不允许跨页截断**，放不下整题移到下一页。中文按字形轮廓绘制，无字体子集化的乱码问题。 |
| **P8 讲解超纲** | v2 才有。约束现在就定下：讲解必须**按 `child.grade` 裁剪方法**，不得使用方程解应用题（小学 3–4 年级未学）、不得使用超纲公式。实现时作为 prompt 硬约束 + 输出后校验关键词。 |
| **P9 响应慢** | 检出 12s、RPM 3。已通过**后台异步**规避：圈题页打开即可手动圈，框到了再铺上去，失败静默降级。用户感知不到 12s。RPM 3 在单家庭够用，家长群试用会撞——属于《模型选型》要解决的问题，不是产品设计问题。 |

---

## 七、明确不做

| 不做 | 理由 |
|---|---|
| 皮肤 / 金币 / 打卡任务 | §5.2 明确列为负面：诱导孩子为奖励敷衍学习功能。与产品目的直接冲突。 |
| 会员墙 / 自动续费 | 试用期不收钱（《商业化瓶颈》§十一）。收钱即触发经营性备案。 |
| 选科目 / 打标签 / 写备注 | 都是摩擦。只做小学数学，科目字段没有存在意义。知识点标注见《系统架构》§3.4。 |
| 相似题推荐 | 版权风险 + 需要题库。v2 之后再谈。 |

---

## 八、落地顺序

| 顺序 | 事项 | 工作量 | 为什么是这个顺序 |
|---|---|---|---|
| 1 | §二 答案改可选 + 回收时补录 | 半天 | 唯一会在两周内导致停用的问题 |
| 2 | §三 落库 `box_origin` / `detected_boxes` | 2 小时 | 每晚一天就少一天的训练数据 |
| 3 | 用真实作业照片跑检出 | 1 小时 | 当前 12/12 是合成图跑出来的，不算数 |
| 4 | §2.5 框住答案区 | 半天 | 看第 1 步之后家长实际怎么用再定 |
| 5 | §四 `correction` 表 + 可追溯 | 1 天 | 接批改模型**之前**必须建好 |
| 6 | §5.2 假掌握 | —— | 攒够两个月数据再说 |
